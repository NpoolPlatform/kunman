// Code generated by ent, DO NOT EDIT.

package generated

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/kunman/middleware/appuser/db/ent/generated/appusercontrol"
	"github.com/NpoolPlatform/kunman/middleware/appuser/db/ent/generated/predicate"
)

// AppUserControlQuery is the builder for querying AppUserControl entities.
type AppUserControlQuery struct {
	config
	ctx        *QueryContext
	order      []appusercontrol.OrderOption
	inters     []Interceptor
	predicates []predicate.AppUserControl
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AppUserControlQuery builder.
func (aucq *AppUserControlQuery) Where(ps ...predicate.AppUserControl) *AppUserControlQuery {
	aucq.predicates = append(aucq.predicates, ps...)
	return aucq
}

// Limit the number of records to be returned by this query.
func (aucq *AppUserControlQuery) Limit(limit int) *AppUserControlQuery {
	aucq.ctx.Limit = &limit
	return aucq
}

// Offset to start from.
func (aucq *AppUserControlQuery) Offset(offset int) *AppUserControlQuery {
	aucq.ctx.Offset = &offset
	return aucq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aucq *AppUserControlQuery) Unique(unique bool) *AppUserControlQuery {
	aucq.ctx.Unique = &unique
	return aucq
}

// Order specifies how the records should be ordered.
func (aucq *AppUserControlQuery) Order(o ...appusercontrol.OrderOption) *AppUserControlQuery {
	aucq.order = append(aucq.order, o...)
	return aucq
}

// First returns the first AppUserControl entity from the query.
// Returns a *NotFoundError when no AppUserControl was found.
func (aucq *AppUserControlQuery) First(ctx context.Context) (*AppUserControl, error) {
	nodes, err := aucq.Limit(1).All(setContextOp(ctx, aucq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{appusercontrol.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aucq *AppUserControlQuery) FirstX(ctx context.Context) *AppUserControl {
	node, err := aucq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AppUserControl ID from the query.
// Returns a *NotFoundError when no AppUserControl ID was found.
func (aucq *AppUserControlQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = aucq.Limit(1).IDs(setContextOp(ctx, aucq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{appusercontrol.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aucq *AppUserControlQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := aucq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AppUserControl entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AppUserControl entity is found.
// Returns a *NotFoundError when no AppUserControl entities are found.
func (aucq *AppUserControlQuery) Only(ctx context.Context) (*AppUserControl, error) {
	nodes, err := aucq.Limit(2).All(setContextOp(ctx, aucq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{appusercontrol.Label}
	default:
		return nil, &NotSingularError{appusercontrol.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aucq *AppUserControlQuery) OnlyX(ctx context.Context) *AppUserControl {
	node, err := aucq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AppUserControl ID in the query.
// Returns a *NotSingularError when more than one AppUserControl ID is found.
// Returns a *NotFoundError when no entities are found.
func (aucq *AppUserControlQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = aucq.Limit(2).IDs(setContextOp(ctx, aucq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{appusercontrol.Label}
	default:
		err = &NotSingularError{appusercontrol.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aucq *AppUserControlQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := aucq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AppUserControls.
func (aucq *AppUserControlQuery) All(ctx context.Context) ([]*AppUserControl, error) {
	ctx = setContextOp(ctx, aucq.ctx, ent.OpQueryAll)
	if err := aucq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AppUserControl, *AppUserControlQuery]()
	return withInterceptors[[]*AppUserControl](ctx, aucq, qr, aucq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aucq *AppUserControlQuery) AllX(ctx context.Context) []*AppUserControl {
	nodes, err := aucq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AppUserControl IDs.
func (aucq *AppUserControlQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if aucq.ctx.Unique == nil && aucq.path != nil {
		aucq.Unique(true)
	}
	ctx = setContextOp(ctx, aucq.ctx, ent.OpQueryIDs)
	if err = aucq.Select(appusercontrol.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aucq *AppUserControlQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := aucq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aucq *AppUserControlQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aucq.ctx, ent.OpQueryCount)
	if err := aucq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aucq, querierCount[*AppUserControlQuery](), aucq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aucq *AppUserControlQuery) CountX(ctx context.Context) int {
	count, err := aucq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aucq *AppUserControlQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aucq.ctx, ent.OpQueryExist)
	switch _, err := aucq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("generated: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aucq *AppUserControlQuery) ExistX(ctx context.Context) bool {
	exist, err := aucq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AppUserControlQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aucq *AppUserControlQuery) Clone() *AppUserControlQuery {
	if aucq == nil {
		return nil
	}
	return &AppUserControlQuery{
		config:     aucq.config,
		ctx:        aucq.ctx.Clone(),
		order:      append([]appusercontrol.OrderOption{}, aucq.order...),
		inters:     append([]Interceptor{}, aucq.inters...),
		predicates: append([]predicate.AppUserControl{}, aucq.predicates...),
		// clone intermediate query.
		sql:       aucq.sql.Clone(),
		path:      aucq.path,
		modifiers: append([]func(*sql.Selector){}, aucq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AppUserControl.Query().
//		GroupBy(appusercontrol.FieldCreatedAt).
//		Aggregate(generated.Count()).
//		Scan(ctx, &v)
func (aucq *AppUserControlQuery) GroupBy(field string, fields ...string) *AppUserControlGroupBy {
	aucq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AppUserControlGroupBy{build: aucq}
	grbuild.flds = &aucq.ctx.Fields
	grbuild.label = appusercontrol.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.AppUserControl.Query().
//		Select(appusercontrol.FieldCreatedAt).
//		Scan(ctx, &v)
func (aucq *AppUserControlQuery) Select(fields ...string) *AppUserControlSelect {
	aucq.ctx.Fields = append(aucq.ctx.Fields, fields...)
	sbuild := &AppUserControlSelect{AppUserControlQuery: aucq}
	sbuild.label = appusercontrol.Label
	sbuild.flds, sbuild.scan = &aucq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AppUserControlSelect configured with the given aggregations.
func (aucq *AppUserControlQuery) Aggregate(fns ...AggregateFunc) *AppUserControlSelect {
	return aucq.Select().Aggregate(fns...)
}

func (aucq *AppUserControlQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aucq.inters {
		if inter == nil {
			return fmt.Errorf("generated: uninitialized interceptor (forgotten import generated/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aucq); err != nil {
				return err
			}
		}
	}
	for _, f := range aucq.ctx.Fields {
		if !appusercontrol.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("generated: invalid field %q for query", f)}
		}
	}
	if aucq.path != nil {
		prev, err := aucq.path(ctx)
		if err != nil {
			return err
		}
		aucq.sql = prev
	}
	return nil
}

func (aucq *AppUserControlQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AppUserControl, error) {
	var (
		nodes = []*AppUserControl{}
		_spec = aucq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AppUserControl).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AppUserControl{config: aucq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(aucq.modifiers) > 0 {
		_spec.Modifiers = aucq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aucq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (aucq *AppUserControlQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aucq.querySpec()
	if len(aucq.modifiers) > 0 {
		_spec.Modifiers = aucq.modifiers
	}
	_spec.Node.Columns = aucq.ctx.Fields
	if len(aucq.ctx.Fields) > 0 {
		_spec.Unique = aucq.ctx.Unique != nil && *aucq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, aucq.driver, _spec)
}

func (aucq *AppUserControlQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(appusercontrol.Table, appusercontrol.Columns, sqlgraph.NewFieldSpec(appusercontrol.FieldID, field.TypeUint32))
	_spec.From = aucq.sql
	if unique := aucq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aucq.path != nil {
		_spec.Unique = true
	}
	if fields := aucq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, appusercontrol.FieldID)
		for i := range fields {
			if fields[i] != appusercontrol.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := aucq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aucq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aucq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aucq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aucq *AppUserControlQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aucq.driver.Dialect())
	t1 := builder.Table(appusercontrol.Table)
	columns := aucq.ctx.Fields
	if len(columns) == 0 {
		columns = appusercontrol.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aucq.sql != nil {
		selector = aucq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aucq.ctx.Unique != nil && *aucq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range aucq.modifiers {
		m(selector)
	}
	for _, p := range aucq.predicates {
		p(selector)
	}
	for _, p := range aucq.order {
		p(selector)
	}
	if offset := aucq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aucq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (aucq *AppUserControlQuery) ForUpdate(opts ...sql.LockOption) *AppUserControlQuery {
	if aucq.driver.Dialect() == dialect.Postgres {
		aucq.Unique(false)
	}
	aucq.modifiers = append(aucq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return aucq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (aucq *AppUserControlQuery) ForShare(opts ...sql.LockOption) *AppUserControlQuery {
	if aucq.driver.Dialect() == dialect.Postgres {
		aucq.Unique(false)
	}
	aucq.modifiers = append(aucq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return aucq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (aucq *AppUserControlQuery) Modify(modifiers ...func(s *sql.Selector)) *AppUserControlSelect {
	aucq.modifiers = append(aucq.modifiers, modifiers...)
	return aucq.Select()
}

// AppUserControlGroupBy is the group-by builder for AppUserControl entities.
type AppUserControlGroupBy struct {
	selector
	build *AppUserControlQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (aucgb *AppUserControlGroupBy) Aggregate(fns ...AggregateFunc) *AppUserControlGroupBy {
	aucgb.fns = append(aucgb.fns, fns...)
	return aucgb
}

// Scan applies the selector query and scans the result into the given value.
func (aucgb *AppUserControlGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aucgb.build.ctx, ent.OpQueryGroupBy)
	if err := aucgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AppUserControlQuery, *AppUserControlGroupBy](ctx, aucgb.build, aucgb, aucgb.build.inters, v)
}

func (aucgb *AppUserControlGroupBy) sqlScan(ctx context.Context, root *AppUserControlQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(aucgb.fns))
	for _, fn := range aucgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*aucgb.flds)+len(aucgb.fns))
		for _, f := range *aucgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*aucgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aucgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AppUserControlSelect is the builder for selecting fields of AppUserControl entities.
type AppUserControlSelect struct {
	*AppUserControlQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (aucs *AppUserControlSelect) Aggregate(fns ...AggregateFunc) *AppUserControlSelect {
	aucs.fns = append(aucs.fns, fns...)
	return aucs
}

// Scan applies the selector query and scans the result into the given value.
func (aucs *AppUserControlSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aucs.ctx, ent.OpQuerySelect)
	if err := aucs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AppUserControlQuery, *AppUserControlSelect](ctx, aucs.AppUserControlQuery, aucs, aucs.inters, v)
}

func (aucs *AppUserControlSelect) sqlScan(ctx context.Context, root *AppUserControlQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(aucs.fns))
	for _, fn := range aucs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*aucs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aucs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (aucs *AppUserControlSelect) Modify(modifiers ...func(s *sql.Selector)) *AppUserControlSelect {
	aucs.modifiers = append(aucs.modifiers, modifiers...)
	return aucs
}
