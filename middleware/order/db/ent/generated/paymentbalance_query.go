// Code generated by ent, DO NOT EDIT.

package generated

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/kunman/middleware/order/db/ent/generated/paymentbalance"
	"github.com/NpoolPlatform/kunman/middleware/order/db/ent/generated/predicate"
)

// PaymentBalanceQuery is the builder for querying PaymentBalance entities.
type PaymentBalanceQuery struct {
	config
	ctx        *QueryContext
	order      []paymentbalance.OrderOption
	inters     []Interceptor
	predicates []predicate.PaymentBalance
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PaymentBalanceQuery builder.
func (pbq *PaymentBalanceQuery) Where(ps ...predicate.PaymentBalance) *PaymentBalanceQuery {
	pbq.predicates = append(pbq.predicates, ps...)
	return pbq
}

// Limit the number of records to be returned by this query.
func (pbq *PaymentBalanceQuery) Limit(limit int) *PaymentBalanceQuery {
	pbq.ctx.Limit = &limit
	return pbq
}

// Offset to start from.
func (pbq *PaymentBalanceQuery) Offset(offset int) *PaymentBalanceQuery {
	pbq.ctx.Offset = &offset
	return pbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pbq *PaymentBalanceQuery) Unique(unique bool) *PaymentBalanceQuery {
	pbq.ctx.Unique = &unique
	return pbq
}

// Order specifies how the records should be ordered.
func (pbq *PaymentBalanceQuery) Order(o ...paymentbalance.OrderOption) *PaymentBalanceQuery {
	pbq.order = append(pbq.order, o...)
	return pbq
}

// First returns the first PaymentBalance entity from the query.
// Returns a *NotFoundError when no PaymentBalance was found.
func (pbq *PaymentBalanceQuery) First(ctx context.Context) (*PaymentBalance, error) {
	nodes, err := pbq.Limit(1).All(setContextOp(ctx, pbq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{paymentbalance.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pbq *PaymentBalanceQuery) FirstX(ctx context.Context) *PaymentBalance {
	node, err := pbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PaymentBalance ID from the query.
// Returns a *NotFoundError when no PaymentBalance ID was found.
func (pbq *PaymentBalanceQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = pbq.Limit(1).IDs(setContextOp(ctx, pbq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{paymentbalance.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pbq *PaymentBalanceQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := pbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PaymentBalance entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PaymentBalance entity is found.
// Returns a *NotFoundError when no PaymentBalance entities are found.
func (pbq *PaymentBalanceQuery) Only(ctx context.Context) (*PaymentBalance, error) {
	nodes, err := pbq.Limit(2).All(setContextOp(ctx, pbq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{paymentbalance.Label}
	default:
		return nil, &NotSingularError{paymentbalance.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pbq *PaymentBalanceQuery) OnlyX(ctx context.Context) *PaymentBalance {
	node, err := pbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PaymentBalance ID in the query.
// Returns a *NotSingularError when more than one PaymentBalance ID is found.
// Returns a *NotFoundError when no entities are found.
func (pbq *PaymentBalanceQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = pbq.Limit(2).IDs(setContextOp(ctx, pbq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{paymentbalance.Label}
	default:
		err = &NotSingularError{paymentbalance.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pbq *PaymentBalanceQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := pbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PaymentBalances.
func (pbq *PaymentBalanceQuery) All(ctx context.Context) ([]*PaymentBalance, error) {
	ctx = setContextOp(ctx, pbq.ctx, ent.OpQueryAll)
	if err := pbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PaymentBalance, *PaymentBalanceQuery]()
	return withInterceptors[[]*PaymentBalance](ctx, pbq, qr, pbq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pbq *PaymentBalanceQuery) AllX(ctx context.Context) []*PaymentBalance {
	nodes, err := pbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PaymentBalance IDs.
func (pbq *PaymentBalanceQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if pbq.ctx.Unique == nil && pbq.path != nil {
		pbq.Unique(true)
	}
	ctx = setContextOp(ctx, pbq.ctx, ent.OpQueryIDs)
	if err = pbq.Select(paymentbalance.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pbq *PaymentBalanceQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := pbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pbq *PaymentBalanceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pbq.ctx, ent.OpQueryCount)
	if err := pbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pbq, querierCount[*PaymentBalanceQuery](), pbq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pbq *PaymentBalanceQuery) CountX(ctx context.Context) int {
	count, err := pbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pbq *PaymentBalanceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pbq.ctx, ent.OpQueryExist)
	switch _, err := pbq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("generated: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pbq *PaymentBalanceQuery) ExistX(ctx context.Context) bool {
	exist, err := pbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PaymentBalanceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pbq *PaymentBalanceQuery) Clone() *PaymentBalanceQuery {
	if pbq == nil {
		return nil
	}
	return &PaymentBalanceQuery{
		config:     pbq.config,
		ctx:        pbq.ctx.Clone(),
		order:      append([]paymentbalance.OrderOption{}, pbq.order...),
		inters:     append([]Interceptor{}, pbq.inters...),
		predicates: append([]predicate.PaymentBalance{}, pbq.predicates...),
		// clone intermediate query.
		sql:       pbq.sql.Clone(),
		path:      pbq.path,
		modifiers: append([]func(*sql.Selector){}, pbq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PaymentBalance.Query().
//		GroupBy(paymentbalance.FieldEntID).
//		Aggregate(generated.Count()).
//		Scan(ctx, &v)
func (pbq *PaymentBalanceQuery) GroupBy(field string, fields ...string) *PaymentBalanceGroupBy {
	pbq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PaymentBalanceGroupBy{build: pbq}
	grbuild.flds = &pbq.ctx.Fields
	grbuild.label = paymentbalance.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//	}
//
//	client.PaymentBalance.Query().
//		Select(paymentbalance.FieldEntID).
//		Scan(ctx, &v)
func (pbq *PaymentBalanceQuery) Select(fields ...string) *PaymentBalanceSelect {
	pbq.ctx.Fields = append(pbq.ctx.Fields, fields...)
	sbuild := &PaymentBalanceSelect{PaymentBalanceQuery: pbq}
	sbuild.label = paymentbalance.Label
	sbuild.flds, sbuild.scan = &pbq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PaymentBalanceSelect configured with the given aggregations.
func (pbq *PaymentBalanceQuery) Aggregate(fns ...AggregateFunc) *PaymentBalanceSelect {
	return pbq.Select().Aggregate(fns...)
}

func (pbq *PaymentBalanceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pbq.inters {
		if inter == nil {
			return fmt.Errorf("generated: uninitialized interceptor (forgotten import generated/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pbq); err != nil {
				return err
			}
		}
	}
	for _, f := range pbq.ctx.Fields {
		if !paymentbalance.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("generated: invalid field %q for query", f)}
		}
	}
	if pbq.path != nil {
		prev, err := pbq.path(ctx)
		if err != nil {
			return err
		}
		pbq.sql = prev
	}
	return nil
}

func (pbq *PaymentBalanceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PaymentBalance, error) {
	var (
		nodes = []*PaymentBalance{}
		_spec = pbq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PaymentBalance).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PaymentBalance{config: pbq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(pbq.modifiers) > 0 {
		_spec.Modifiers = pbq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (pbq *PaymentBalanceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pbq.querySpec()
	if len(pbq.modifiers) > 0 {
		_spec.Modifiers = pbq.modifiers
	}
	_spec.Node.Columns = pbq.ctx.Fields
	if len(pbq.ctx.Fields) > 0 {
		_spec.Unique = pbq.ctx.Unique != nil && *pbq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pbq.driver, _spec)
}

func (pbq *PaymentBalanceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(paymentbalance.Table, paymentbalance.Columns, sqlgraph.NewFieldSpec(paymentbalance.FieldID, field.TypeUint32))
	_spec.From = pbq.sql
	if unique := pbq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pbq.path != nil {
		_spec.Unique = true
	}
	if fields := pbq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, paymentbalance.FieldID)
		for i := range fields {
			if fields[i] != paymentbalance.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pbq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pbq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pbq *PaymentBalanceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pbq.driver.Dialect())
	t1 := builder.Table(paymentbalance.Table)
	columns := pbq.ctx.Fields
	if len(columns) == 0 {
		columns = paymentbalance.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pbq.sql != nil {
		selector = pbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pbq.ctx.Unique != nil && *pbq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range pbq.modifiers {
		m(selector)
	}
	for _, p := range pbq.predicates {
		p(selector)
	}
	for _, p := range pbq.order {
		p(selector)
	}
	if offset := pbq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pbq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (pbq *PaymentBalanceQuery) ForUpdate(opts ...sql.LockOption) *PaymentBalanceQuery {
	if pbq.driver.Dialect() == dialect.Postgres {
		pbq.Unique(false)
	}
	pbq.modifiers = append(pbq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return pbq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (pbq *PaymentBalanceQuery) ForShare(opts ...sql.LockOption) *PaymentBalanceQuery {
	if pbq.driver.Dialect() == dialect.Postgres {
		pbq.Unique(false)
	}
	pbq.modifiers = append(pbq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return pbq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pbq *PaymentBalanceQuery) Modify(modifiers ...func(s *sql.Selector)) *PaymentBalanceSelect {
	pbq.modifiers = append(pbq.modifiers, modifiers...)
	return pbq.Select()
}

// PaymentBalanceGroupBy is the group-by builder for PaymentBalance entities.
type PaymentBalanceGroupBy struct {
	selector
	build *PaymentBalanceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pbgb *PaymentBalanceGroupBy) Aggregate(fns ...AggregateFunc) *PaymentBalanceGroupBy {
	pbgb.fns = append(pbgb.fns, fns...)
	return pbgb
}

// Scan applies the selector query and scans the result into the given value.
func (pbgb *PaymentBalanceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pbgb.build.ctx, ent.OpQueryGroupBy)
	if err := pbgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PaymentBalanceQuery, *PaymentBalanceGroupBy](ctx, pbgb.build, pbgb, pbgb.build.inters, v)
}

func (pbgb *PaymentBalanceGroupBy) sqlScan(ctx context.Context, root *PaymentBalanceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pbgb.fns))
	for _, fn := range pbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pbgb.flds)+len(pbgb.fns))
		for _, f := range *pbgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pbgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pbgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PaymentBalanceSelect is the builder for selecting fields of PaymentBalance entities.
type PaymentBalanceSelect struct {
	*PaymentBalanceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pbs *PaymentBalanceSelect) Aggregate(fns ...AggregateFunc) *PaymentBalanceSelect {
	pbs.fns = append(pbs.fns, fns...)
	return pbs
}

// Scan applies the selector query and scans the result into the given value.
func (pbs *PaymentBalanceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pbs.ctx, ent.OpQuerySelect)
	if err := pbs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PaymentBalanceQuery, *PaymentBalanceSelect](ctx, pbs.PaymentBalanceQuery, pbs, pbs.inters, v)
}

func (pbs *PaymentBalanceSelect) sqlScan(ctx context.Context, root *PaymentBalanceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pbs.fns))
	for _, fn := range pbs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pbs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pbs *PaymentBalanceSelect) Modify(modifiers ...func(s *sql.Selector)) *PaymentBalanceSelect {
	pbs.modifiers = append(pbs.modifiers, modifiers...)
	return pbs
}
