// Code generated by ent, DO NOT EDIT.

package generated

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/kunman/middleware/order/db/ent/generated/paymentbase"
	"github.com/NpoolPlatform/kunman/middleware/order/db/ent/generated/predicate"
)

// PaymentBaseQuery is the builder for querying PaymentBase entities.
type PaymentBaseQuery struct {
	config
	ctx        *QueryContext
	order      []paymentbase.OrderOption
	inters     []Interceptor
	predicates []predicate.PaymentBase
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PaymentBaseQuery builder.
func (pbq *PaymentBaseQuery) Where(ps ...predicate.PaymentBase) *PaymentBaseQuery {
	pbq.predicates = append(pbq.predicates, ps...)
	return pbq
}

// Limit the number of records to be returned by this query.
func (pbq *PaymentBaseQuery) Limit(limit int) *PaymentBaseQuery {
	pbq.ctx.Limit = &limit
	return pbq
}

// Offset to start from.
func (pbq *PaymentBaseQuery) Offset(offset int) *PaymentBaseQuery {
	pbq.ctx.Offset = &offset
	return pbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pbq *PaymentBaseQuery) Unique(unique bool) *PaymentBaseQuery {
	pbq.ctx.Unique = &unique
	return pbq
}

// Order specifies how the records should be ordered.
func (pbq *PaymentBaseQuery) Order(o ...paymentbase.OrderOption) *PaymentBaseQuery {
	pbq.order = append(pbq.order, o...)
	return pbq
}

// First returns the first PaymentBase entity from the query.
// Returns a *NotFoundError when no PaymentBase was found.
func (pbq *PaymentBaseQuery) First(ctx context.Context) (*PaymentBase, error) {
	nodes, err := pbq.Limit(1).All(setContextOp(ctx, pbq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{paymentbase.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pbq *PaymentBaseQuery) FirstX(ctx context.Context) *PaymentBase {
	node, err := pbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PaymentBase ID from the query.
// Returns a *NotFoundError when no PaymentBase ID was found.
func (pbq *PaymentBaseQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = pbq.Limit(1).IDs(setContextOp(ctx, pbq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{paymentbase.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pbq *PaymentBaseQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := pbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PaymentBase entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PaymentBase entity is found.
// Returns a *NotFoundError when no PaymentBase entities are found.
func (pbq *PaymentBaseQuery) Only(ctx context.Context) (*PaymentBase, error) {
	nodes, err := pbq.Limit(2).All(setContextOp(ctx, pbq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{paymentbase.Label}
	default:
		return nil, &NotSingularError{paymentbase.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pbq *PaymentBaseQuery) OnlyX(ctx context.Context) *PaymentBase {
	node, err := pbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PaymentBase ID in the query.
// Returns a *NotSingularError when more than one PaymentBase ID is found.
// Returns a *NotFoundError when no entities are found.
func (pbq *PaymentBaseQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = pbq.Limit(2).IDs(setContextOp(ctx, pbq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{paymentbase.Label}
	default:
		err = &NotSingularError{paymentbase.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pbq *PaymentBaseQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := pbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PaymentBases.
func (pbq *PaymentBaseQuery) All(ctx context.Context) ([]*PaymentBase, error) {
	ctx = setContextOp(ctx, pbq.ctx, ent.OpQueryAll)
	if err := pbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PaymentBase, *PaymentBaseQuery]()
	return withInterceptors[[]*PaymentBase](ctx, pbq, qr, pbq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pbq *PaymentBaseQuery) AllX(ctx context.Context) []*PaymentBase {
	nodes, err := pbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PaymentBase IDs.
func (pbq *PaymentBaseQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if pbq.ctx.Unique == nil && pbq.path != nil {
		pbq.Unique(true)
	}
	ctx = setContextOp(ctx, pbq.ctx, ent.OpQueryIDs)
	if err = pbq.Select(paymentbase.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pbq *PaymentBaseQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := pbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pbq *PaymentBaseQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pbq.ctx, ent.OpQueryCount)
	if err := pbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pbq, querierCount[*PaymentBaseQuery](), pbq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pbq *PaymentBaseQuery) CountX(ctx context.Context) int {
	count, err := pbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pbq *PaymentBaseQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pbq.ctx, ent.OpQueryExist)
	switch _, err := pbq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("generated: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pbq *PaymentBaseQuery) ExistX(ctx context.Context) bool {
	exist, err := pbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PaymentBaseQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pbq *PaymentBaseQuery) Clone() *PaymentBaseQuery {
	if pbq == nil {
		return nil
	}
	return &PaymentBaseQuery{
		config:     pbq.config,
		ctx:        pbq.ctx.Clone(),
		order:      append([]paymentbase.OrderOption{}, pbq.order...),
		inters:     append([]Interceptor{}, pbq.inters...),
		predicates: append([]predicate.PaymentBase{}, pbq.predicates...),
		// clone intermediate query.
		sql:       pbq.sql.Clone(),
		path:      pbq.path,
		modifiers: append([]func(*sql.Selector){}, pbq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PaymentBase.Query().
//		GroupBy(paymentbase.FieldEntID).
//		Aggregate(generated.Count()).
//		Scan(ctx, &v)
func (pbq *PaymentBaseQuery) GroupBy(field string, fields ...string) *PaymentBaseGroupBy {
	pbq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PaymentBaseGroupBy{build: pbq}
	grbuild.flds = &pbq.ctx.Fields
	grbuild.label = paymentbase.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//	}
//
//	client.PaymentBase.Query().
//		Select(paymentbase.FieldEntID).
//		Scan(ctx, &v)
func (pbq *PaymentBaseQuery) Select(fields ...string) *PaymentBaseSelect {
	pbq.ctx.Fields = append(pbq.ctx.Fields, fields...)
	sbuild := &PaymentBaseSelect{PaymentBaseQuery: pbq}
	sbuild.label = paymentbase.Label
	sbuild.flds, sbuild.scan = &pbq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PaymentBaseSelect configured with the given aggregations.
func (pbq *PaymentBaseQuery) Aggregate(fns ...AggregateFunc) *PaymentBaseSelect {
	return pbq.Select().Aggregate(fns...)
}

func (pbq *PaymentBaseQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pbq.inters {
		if inter == nil {
			return fmt.Errorf("generated: uninitialized interceptor (forgotten import generated/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pbq); err != nil {
				return err
			}
		}
	}
	for _, f := range pbq.ctx.Fields {
		if !paymentbase.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("generated: invalid field %q for query", f)}
		}
	}
	if pbq.path != nil {
		prev, err := pbq.path(ctx)
		if err != nil {
			return err
		}
		pbq.sql = prev
	}
	return nil
}

func (pbq *PaymentBaseQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PaymentBase, error) {
	var (
		nodes = []*PaymentBase{}
		_spec = pbq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PaymentBase).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PaymentBase{config: pbq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(pbq.modifiers) > 0 {
		_spec.Modifiers = pbq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (pbq *PaymentBaseQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pbq.querySpec()
	if len(pbq.modifiers) > 0 {
		_spec.Modifiers = pbq.modifiers
	}
	_spec.Node.Columns = pbq.ctx.Fields
	if len(pbq.ctx.Fields) > 0 {
		_spec.Unique = pbq.ctx.Unique != nil && *pbq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pbq.driver, _spec)
}

func (pbq *PaymentBaseQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(paymentbase.Table, paymentbase.Columns, sqlgraph.NewFieldSpec(paymentbase.FieldID, field.TypeUint32))
	_spec.From = pbq.sql
	if unique := pbq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pbq.path != nil {
		_spec.Unique = true
	}
	if fields := pbq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, paymentbase.FieldID)
		for i := range fields {
			if fields[i] != paymentbase.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pbq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pbq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pbq *PaymentBaseQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pbq.driver.Dialect())
	t1 := builder.Table(paymentbase.Table)
	columns := pbq.ctx.Fields
	if len(columns) == 0 {
		columns = paymentbase.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pbq.sql != nil {
		selector = pbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pbq.ctx.Unique != nil && *pbq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range pbq.modifiers {
		m(selector)
	}
	for _, p := range pbq.predicates {
		p(selector)
	}
	for _, p := range pbq.order {
		p(selector)
	}
	if offset := pbq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pbq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (pbq *PaymentBaseQuery) ForUpdate(opts ...sql.LockOption) *PaymentBaseQuery {
	if pbq.driver.Dialect() == dialect.Postgres {
		pbq.Unique(false)
	}
	pbq.modifiers = append(pbq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return pbq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (pbq *PaymentBaseQuery) ForShare(opts ...sql.LockOption) *PaymentBaseQuery {
	if pbq.driver.Dialect() == dialect.Postgres {
		pbq.Unique(false)
	}
	pbq.modifiers = append(pbq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return pbq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pbq *PaymentBaseQuery) Modify(modifiers ...func(s *sql.Selector)) *PaymentBaseSelect {
	pbq.modifiers = append(pbq.modifiers, modifiers...)
	return pbq.Select()
}

// PaymentBaseGroupBy is the group-by builder for PaymentBase entities.
type PaymentBaseGroupBy struct {
	selector
	build *PaymentBaseQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pbgb *PaymentBaseGroupBy) Aggregate(fns ...AggregateFunc) *PaymentBaseGroupBy {
	pbgb.fns = append(pbgb.fns, fns...)
	return pbgb
}

// Scan applies the selector query and scans the result into the given value.
func (pbgb *PaymentBaseGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pbgb.build.ctx, ent.OpQueryGroupBy)
	if err := pbgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PaymentBaseQuery, *PaymentBaseGroupBy](ctx, pbgb.build, pbgb, pbgb.build.inters, v)
}

func (pbgb *PaymentBaseGroupBy) sqlScan(ctx context.Context, root *PaymentBaseQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pbgb.fns))
	for _, fn := range pbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pbgb.flds)+len(pbgb.fns))
		for _, f := range *pbgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pbgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pbgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PaymentBaseSelect is the builder for selecting fields of PaymentBase entities.
type PaymentBaseSelect struct {
	*PaymentBaseQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pbs *PaymentBaseSelect) Aggregate(fns ...AggregateFunc) *PaymentBaseSelect {
	pbs.fns = append(pbs.fns, fns...)
	return pbs
}

// Scan applies the selector query and scans the result into the given value.
func (pbs *PaymentBaseSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pbs.ctx, ent.OpQuerySelect)
	if err := pbs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PaymentBaseQuery, *PaymentBaseSelect](ctx, pbs.PaymentBaseQuery, pbs, pbs.inters, v)
}

func (pbs *PaymentBaseSelect) sqlScan(ctx context.Context, root *PaymentBaseQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pbs.fns))
	for _, fn := range pbs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pbs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pbs *PaymentBaseSelect) Modify(modifiers ...func(s *sql.Selector)) *PaymentBaseSelect {
	pbs.modifiers = append(pbs.modifiers, modifiers...)
	return pbs
}
