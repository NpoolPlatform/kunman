// Code generated by ent, DO NOT EDIT.

package generated

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/kunman/middleware/order/db/ent/generated/orderlock"
	"github.com/NpoolPlatform/kunman/middleware/order/db/ent/generated/predicate"
)

// OrderLockQuery is the builder for querying OrderLock entities.
type OrderLockQuery struct {
	config
	ctx        *QueryContext
	order      []orderlock.OrderOption
	inters     []Interceptor
	predicates []predicate.OrderLock
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OrderLockQuery builder.
func (olq *OrderLockQuery) Where(ps ...predicate.OrderLock) *OrderLockQuery {
	olq.predicates = append(olq.predicates, ps...)
	return olq
}

// Limit the number of records to be returned by this query.
func (olq *OrderLockQuery) Limit(limit int) *OrderLockQuery {
	olq.ctx.Limit = &limit
	return olq
}

// Offset to start from.
func (olq *OrderLockQuery) Offset(offset int) *OrderLockQuery {
	olq.ctx.Offset = &offset
	return olq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (olq *OrderLockQuery) Unique(unique bool) *OrderLockQuery {
	olq.ctx.Unique = &unique
	return olq
}

// Order specifies how the records should be ordered.
func (olq *OrderLockQuery) Order(o ...orderlock.OrderOption) *OrderLockQuery {
	olq.order = append(olq.order, o...)
	return olq
}

// First returns the first OrderLock entity from the query.
// Returns a *NotFoundError when no OrderLock was found.
func (olq *OrderLockQuery) First(ctx context.Context) (*OrderLock, error) {
	nodes, err := olq.Limit(1).All(setContextOp(ctx, olq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{orderlock.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (olq *OrderLockQuery) FirstX(ctx context.Context) *OrderLock {
	node, err := olq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OrderLock ID from the query.
// Returns a *NotFoundError when no OrderLock ID was found.
func (olq *OrderLockQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = olq.Limit(1).IDs(setContextOp(ctx, olq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{orderlock.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (olq *OrderLockQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := olq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OrderLock entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OrderLock entity is found.
// Returns a *NotFoundError when no OrderLock entities are found.
func (olq *OrderLockQuery) Only(ctx context.Context) (*OrderLock, error) {
	nodes, err := olq.Limit(2).All(setContextOp(ctx, olq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{orderlock.Label}
	default:
		return nil, &NotSingularError{orderlock.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (olq *OrderLockQuery) OnlyX(ctx context.Context) *OrderLock {
	node, err := olq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OrderLock ID in the query.
// Returns a *NotSingularError when more than one OrderLock ID is found.
// Returns a *NotFoundError when no entities are found.
func (olq *OrderLockQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = olq.Limit(2).IDs(setContextOp(ctx, olq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{orderlock.Label}
	default:
		err = &NotSingularError{orderlock.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (olq *OrderLockQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := olq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OrderLocks.
func (olq *OrderLockQuery) All(ctx context.Context) ([]*OrderLock, error) {
	ctx = setContextOp(ctx, olq.ctx, ent.OpQueryAll)
	if err := olq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OrderLock, *OrderLockQuery]()
	return withInterceptors[[]*OrderLock](ctx, olq, qr, olq.inters)
}

// AllX is like All, but panics if an error occurs.
func (olq *OrderLockQuery) AllX(ctx context.Context) []*OrderLock {
	nodes, err := olq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OrderLock IDs.
func (olq *OrderLockQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if olq.ctx.Unique == nil && olq.path != nil {
		olq.Unique(true)
	}
	ctx = setContextOp(ctx, olq.ctx, ent.OpQueryIDs)
	if err = olq.Select(orderlock.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (olq *OrderLockQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := olq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (olq *OrderLockQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, olq.ctx, ent.OpQueryCount)
	if err := olq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, olq, querierCount[*OrderLockQuery](), olq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (olq *OrderLockQuery) CountX(ctx context.Context) int {
	count, err := olq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (olq *OrderLockQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, olq.ctx, ent.OpQueryExist)
	switch _, err := olq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("generated: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (olq *OrderLockQuery) ExistX(ctx context.Context) bool {
	exist, err := olq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OrderLockQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (olq *OrderLockQuery) Clone() *OrderLockQuery {
	if olq == nil {
		return nil
	}
	return &OrderLockQuery{
		config:     olq.config,
		ctx:        olq.ctx.Clone(),
		order:      append([]orderlock.OrderOption{}, olq.order...),
		inters:     append([]Interceptor{}, olq.inters...),
		predicates: append([]predicate.OrderLock{}, olq.predicates...),
		// clone intermediate query.
		sql:       olq.sql.Clone(),
		path:      olq.path,
		modifiers: append([]func(*sql.Selector){}, olq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OrderLock.Query().
//		GroupBy(orderlock.FieldEntID).
//		Aggregate(generated.Count()).
//		Scan(ctx, &v)
func (olq *OrderLockQuery) GroupBy(field string, fields ...string) *OrderLockGroupBy {
	olq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OrderLockGroupBy{build: olq}
	grbuild.flds = &olq.ctx.Fields
	grbuild.label = orderlock.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//	}
//
//	client.OrderLock.Query().
//		Select(orderlock.FieldEntID).
//		Scan(ctx, &v)
func (olq *OrderLockQuery) Select(fields ...string) *OrderLockSelect {
	olq.ctx.Fields = append(olq.ctx.Fields, fields...)
	sbuild := &OrderLockSelect{OrderLockQuery: olq}
	sbuild.label = orderlock.Label
	sbuild.flds, sbuild.scan = &olq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OrderLockSelect configured with the given aggregations.
func (olq *OrderLockQuery) Aggregate(fns ...AggregateFunc) *OrderLockSelect {
	return olq.Select().Aggregate(fns...)
}

func (olq *OrderLockQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range olq.inters {
		if inter == nil {
			return fmt.Errorf("generated: uninitialized interceptor (forgotten import generated/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, olq); err != nil {
				return err
			}
		}
	}
	for _, f := range olq.ctx.Fields {
		if !orderlock.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("generated: invalid field %q for query", f)}
		}
	}
	if olq.path != nil {
		prev, err := olq.path(ctx)
		if err != nil {
			return err
		}
		olq.sql = prev
	}
	return nil
}

func (olq *OrderLockQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OrderLock, error) {
	var (
		nodes = []*OrderLock{}
		_spec = olq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OrderLock).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OrderLock{config: olq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(olq.modifiers) > 0 {
		_spec.Modifiers = olq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, olq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (olq *OrderLockQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := olq.querySpec()
	if len(olq.modifiers) > 0 {
		_spec.Modifiers = olq.modifiers
	}
	_spec.Node.Columns = olq.ctx.Fields
	if len(olq.ctx.Fields) > 0 {
		_spec.Unique = olq.ctx.Unique != nil && *olq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, olq.driver, _spec)
}

func (olq *OrderLockQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(orderlock.Table, orderlock.Columns, sqlgraph.NewFieldSpec(orderlock.FieldID, field.TypeUint32))
	_spec.From = olq.sql
	if unique := olq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if olq.path != nil {
		_spec.Unique = true
	}
	if fields := olq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, orderlock.FieldID)
		for i := range fields {
			if fields[i] != orderlock.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := olq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := olq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := olq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := olq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (olq *OrderLockQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(olq.driver.Dialect())
	t1 := builder.Table(orderlock.Table)
	columns := olq.ctx.Fields
	if len(columns) == 0 {
		columns = orderlock.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if olq.sql != nil {
		selector = olq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if olq.ctx.Unique != nil && *olq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range olq.modifiers {
		m(selector)
	}
	for _, p := range olq.predicates {
		p(selector)
	}
	for _, p := range olq.order {
		p(selector)
	}
	if offset := olq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := olq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (olq *OrderLockQuery) ForUpdate(opts ...sql.LockOption) *OrderLockQuery {
	if olq.driver.Dialect() == dialect.Postgres {
		olq.Unique(false)
	}
	olq.modifiers = append(olq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return olq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (olq *OrderLockQuery) ForShare(opts ...sql.LockOption) *OrderLockQuery {
	if olq.driver.Dialect() == dialect.Postgres {
		olq.Unique(false)
	}
	olq.modifiers = append(olq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return olq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (olq *OrderLockQuery) Modify(modifiers ...func(s *sql.Selector)) *OrderLockSelect {
	olq.modifiers = append(olq.modifiers, modifiers...)
	return olq.Select()
}

// OrderLockGroupBy is the group-by builder for OrderLock entities.
type OrderLockGroupBy struct {
	selector
	build *OrderLockQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (olgb *OrderLockGroupBy) Aggregate(fns ...AggregateFunc) *OrderLockGroupBy {
	olgb.fns = append(olgb.fns, fns...)
	return olgb
}

// Scan applies the selector query and scans the result into the given value.
func (olgb *OrderLockGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, olgb.build.ctx, ent.OpQueryGroupBy)
	if err := olgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrderLockQuery, *OrderLockGroupBy](ctx, olgb.build, olgb, olgb.build.inters, v)
}

func (olgb *OrderLockGroupBy) sqlScan(ctx context.Context, root *OrderLockQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(olgb.fns))
	for _, fn := range olgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*olgb.flds)+len(olgb.fns))
		for _, f := range *olgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*olgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := olgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OrderLockSelect is the builder for selecting fields of OrderLock entities.
type OrderLockSelect struct {
	*OrderLockQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ols *OrderLockSelect) Aggregate(fns ...AggregateFunc) *OrderLockSelect {
	ols.fns = append(ols.fns, fns...)
	return ols
}

// Scan applies the selector query and scans the result into the given value.
func (ols *OrderLockSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ols.ctx, ent.OpQuerySelect)
	if err := ols.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrderLockQuery, *OrderLockSelect](ctx, ols.OrderLockQuery, ols, ols.inters, v)
}

func (ols *OrderLockSelect) sqlScan(ctx context.Context, root *OrderLockQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ols.fns))
	for _, fn := range ols.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ols.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ols.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ols *OrderLockSelect) Modify(modifiers ...func(s *sql.Selector)) *OrderLockSelect {
	ols.modifiers = append(ols.modifiers, modifiers...)
	return ols
}
