// Code generated by ent, DO NOT EDIT.

package generated

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/kunman/middleware/order/db/ent/generated/orderbase"
	"github.com/NpoolPlatform/kunman/middleware/order/db/ent/generated/predicate"
)

// OrderBaseQuery is the builder for querying OrderBase entities.
type OrderBaseQuery struct {
	config
	ctx        *QueryContext
	order      []orderbase.OrderOption
	inters     []Interceptor
	predicates []predicate.OrderBase
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OrderBaseQuery builder.
func (obq *OrderBaseQuery) Where(ps ...predicate.OrderBase) *OrderBaseQuery {
	obq.predicates = append(obq.predicates, ps...)
	return obq
}

// Limit the number of records to be returned by this query.
func (obq *OrderBaseQuery) Limit(limit int) *OrderBaseQuery {
	obq.ctx.Limit = &limit
	return obq
}

// Offset to start from.
func (obq *OrderBaseQuery) Offset(offset int) *OrderBaseQuery {
	obq.ctx.Offset = &offset
	return obq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (obq *OrderBaseQuery) Unique(unique bool) *OrderBaseQuery {
	obq.ctx.Unique = &unique
	return obq
}

// Order specifies how the records should be ordered.
func (obq *OrderBaseQuery) Order(o ...orderbase.OrderOption) *OrderBaseQuery {
	obq.order = append(obq.order, o...)
	return obq
}

// First returns the first OrderBase entity from the query.
// Returns a *NotFoundError when no OrderBase was found.
func (obq *OrderBaseQuery) First(ctx context.Context) (*OrderBase, error) {
	nodes, err := obq.Limit(1).All(setContextOp(ctx, obq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{orderbase.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (obq *OrderBaseQuery) FirstX(ctx context.Context) *OrderBase {
	node, err := obq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OrderBase ID from the query.
// Returns a *NotFoundError when no OrderBase ID was found.
func (obq *OrderBaseQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = obq.Limit(1).IDs(setContextOp(ctx, obq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{orderbase.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (obq *OrderBaseQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := obq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OrderBase entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OrderBase entity is found.
// Returns a *NotFoundError when no OrderBase entities are found.
func (obq *OrderBaseQuery) Only(ctx context.Context) (*OrderBase, error) {
	nodes, err := obq.Limit(2).All(setContextOp(ctx, obq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{orderbase.Label}
	default:
		return nil, &NotSingularError{orderbase.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (obq *OrderBaseQuery) OnlyX(ctx context.Context) *OrderBase {
	node, err := obq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OrderBase ID in the query.
// Returns a *NotSingularError when more than one OrderBase ID is found.
// Returns a *NotFoundError when no entities are found.
func (obq *OrderBaseQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = obq.Limit(2).IDs(setContextOp(ctx, obq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{orderbase.Label}
	default:
		err = &NotSingularError{orderbase.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (obq *OrderBaseQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := obq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OrderBases.
func (obq *OrderBaseQuery) All(ctx context.Context) ([]*OrderBase, error) {
	ctx = setContextOp(ctx, obq.ctx, ent.OpQueryAll)
	if err := obq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OrderBase, *OrderBaseQuery]()
	return withInterceptors[[]*OrderBase](ctx, obq, qr, obq.inters)
}

// AllX is like All, but panics if an error occurs.
func (obq *OrderBaseQuery) AllX(ctx context.Context) []*OrderBase {
	nodes, err := obq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OrderBase IDs.
func (obq *OrderBaseQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if obq.ctx.Unique == nil && obq.path != nil {
		obq.Unique(true)
	}
	ctx = setContextOp(ctx, obq.ctx, ent.OpQueryIDs)
	if err = obq.Select(orderbase.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (obq *OrderBaseQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := obq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (obq *OrderBaseQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, obq.ctx, ent.OpQueryCount)
	if err := obq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, obq, querierCount[*OrderBaseQuery](), obq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (obq *OrderBaseQuery) CountX(ctx context.Context) int {
	count, err := obq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (obq *OrderBaseQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, obq.ctx, ent.OpQueryExist)
	switch _, err := obq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("generated: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (obq *OrderBaseQuery) ExistX(ctx context.Context) bool {
	exist, err := obq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OrderBaseQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (obq *OrderBaseQuery) Clone() *OrderBaseQuery {
	if obq == nil {
		return nil
	}
	return &OrderBaseQuery{
		config:     obq.config,
		ctx:        obq.ctx.Clone(),
		order:      append([]orderbase.OrderOption{}, obq.order...),
		inters:     append([]Interceptor{}, obq.inters...),
		predicates: append([]predicate.OrderBase{}, obq.predicates...),
		// clone intermediate query.
		sql:       obq.sql.Clone(),
		path:      obq.path,
		modifiers: append([]func(*sql.Selector){}, obq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OrderBase.Query().
//		GroupBy(orderbase.FieldEntID).
//		Aggregate(generated.Count()).
//		Scan(ctx, &v)
func (obq *OrderBaseQuery) GroupBy(field string, fields ...string) *OrderBaseGroupBy {
	obq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OrderBaseGroupBy{build: obq}
	grbuild.flds = &obq.ctx.Fields
	grbuild.label = orderbase.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//	}
//
//	client.OrderBase.Query().
//		Select(orderbase.FieldEntID).
//		Scan(ctx, &v)
func (obq *OrderBaseQuery) Select(fields ...string) *OrderBaseSelect {
	obq.ctx.Fields = append(obq.ctx.Fields, fields...)
	sbuild := &OrderBaseSelect{OrderBaseQuery: obq}
	sbuild.label = orderbase.Label
	sbuild.flds, sbuild.scan = &obq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OrderBaseSelect configured with the given aggregations.
func (obq *OrderBaseQuery) Aggregate(fns ...AggregateFunc) *OrderBaseSelect {
	return obq.Select().Aggregate(fns...)
}

func (obq *OrderBaseQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range obq.inters {
		if inter == nil {
			return fmt.Errorf("generated: uninitialized interceptor (forgotten import generated/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, obq); err != nil {
				return err
			}
		}
	}
	for _, f := range obq.ctx.Fields {
		if !orderbase.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("generated: invalid field %q for query", f)}
		}
	}
	if obq.path != nil {
		prev, err := obq.path(ctx)
		if err != nil {
			return err
		}
		obq.sql = prev
	}
	return nil
}

func (obq *OrderBaseQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OrderBase, error) {
	var (
		nodes = []*OrderBase{}
		_spec = obq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OrderBase).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OrderBase{config: obq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(obq.modifiers) > 0 {
		_spec.Modifiers = obq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, obq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (obq *OrderBaseQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := obq.querySpec()
	if len(obq.modifiers) > 0 {
		_spec.Modifiers = obq.modifiers
	}
	_spec.Node.Columns = obq.ctx.Fields
	if len(obq.ctx.Fields) > 0 {
		_spec.Unique = obq.ctx.Unique != nil && *obq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, obq.driver, _spec)
}

func (obq *OrderBaseQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(orderbase.Table, orderbase.Columns, sqlgraph.NewFieldSpec(orderbase.FieldID, field.TypeUint32))
	_spec.From = obq.sql
	if unique := obq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if obq.path != nil {
		_spec.Unique = true
	}
	if fields := obq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, orderbase.FieldID)
		for i := range fields {
			if fields[i] != orderbase.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := obq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := obq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := obq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := obq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (obq *OrderBaseQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(obq.driver.Dialect())
	t1 := builder.Table(orderbase.Table)
	columns := obq.ctx.Fields
	if len(columns) == 0 {
		columns = orderbase.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if obq.sql != nil {
		selector = obq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if obq.ctx.Unique != nil && *obq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range obq.modifiers {
		m(selector)
	}
	for _, p := range obq.predicates {
		p(selector)
	}
	for _, p := range obq.order {
		p(selector)
	}
	if offset := obq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := obq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (obq *OrderBaseQuery) ForUpdate(opts ...sql.LockOption) *OrderBaseQuery {
	if obq.driver.Dialect() == dialect.Postgres {
		obq.Unique(false)
	}
	obq.modifiers = append(obq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return obq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (obq *OrderBaseQuery) ForShare(opts ...sql.LockOption) *OrderBaseQuery {
	if obq.driver.Dialect() == dialect.Postgres {
		obq.Unique(false)
	}
	obq.modifiers = append(obq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return obq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (obq *OrderBaseQuery) Modify(modifiers ...func(s *sql.Selector)) *OrderBaseSelect {
	obq.modifiers = append(obq.modifiers, modifiers...)
	return obq.Select()
}

// OrderBaseGroupBy is the group-by builder for OrderBase entities.
type OrderBaseGroupBy struct {
	selector
	build *OrderBaseQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (obgb *OrderBaseGroupBy) Aggregate(fns ...AggregateFunc) *OrderBaseGroupBy {
	obgb.fns = append(obgb.fns, fns...)
	return obgb
}

// Scan applies the selector query and scans the result into the given value.
func (obgb *OrderBaseGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, obgb.build.ctx, ent.OpQueryGroupBy)
	if err := obgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrderBaseQuery, *OrderBaseGroupBy](ctx, obgb.build, obgb, obgb.build.inters, v)
}

func (obgb *OrderBaseGroupBy) sqlScan(ctx context.Context, root *OrderBaseQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(obgb.fns))
	for _, fn := range obgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*obgb.flds)+len(obgb.fns))
		for _, f := range *obgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*obgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := obgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OrderBaseSelect is the builder for selecting fields of OrderBase entities.
type OrderBaseSelect struct {
	*OrderBaseQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (obs *OrderBaseSelect) Aggregate(fns ...AggregateFunc) *OrderBaseSelect {
	obs.fns = append(obs.fns, fns...)
	return obs
}

// Scan applies the selector query and scans the result into the given value.
func (obs *OrderBaseSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, obs.ctx, ent.OpQuerySelect)
	if err := obs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrderBaseQuery, *OrderBaseSelect](ctx, obs.OrderBaseQuery, obs, obs.inters, v)
}

func (obs *OrderBaseSelect) sqlScan(ctx context.Context, root *OrderBaseQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(obs.fns))
	for _, fn := range obs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*obs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := obs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (obs *OrderBaseSelect) Modify(modifiers ...func(s *sql.Selector)) *OrderBaseSelect {
	obs.modifiers = append(obs.modifiers, modifiers...)
	return obs
}
