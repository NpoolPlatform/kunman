// Code generated by ent, DO NOT EDIT.

package generated

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/kunman/middleware/good/db/ent/generated/goodreward"
	"github.com/NpoolPlatform/kunman/middleware/good/db/ent/generated/predicate"
)

// GoodRewardQuery is the builder for querying GoodReward entities.
type GoodRewardQuery struct {
	config
	ctx        *QueryContext
	order      []goodreward.OrderOption
	inters     []Interceptor
	predicates []predicate.GoodReward
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GoodRewardQuery builder.
func (grq *GoodRewardQuery) Where(ps ...predicate.GoodReward) *GoodRewardQuery {
	grq.predicates = append(grq.predicates, ps...)
	return grq
}

// Limit the number of records to be returned by this query.
func (grq *GoodRewardQuery) Limit(limit int) *GoodRewardQuery {
	grq.ctx.Limit = &limit
	return grq
}

// Offset to start from.
func (grq *GoodRewardQuery) Offset(offset int) *GoodRewardQuery {
	grq.ctx.Offset = &offset
	return grq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (grq *GoodRewardQuery) Unique(unique bool) *GoodRewardQuery {
	grq.ctx.Unique = &unique
	return grq
}

// Order specifies how the records should be ordered.
func (grq *GoodRewardQuery) Order(o ...goodreward.OrderOption) *GoodRewardQuery {
	grq.order = append(grq.order, o...)
	return grq
}

// First returns the first GoodReward entity from the query.
// Returns a *NotFoundError when no GoodReward was found.
func (grq *GoodRewardQuery) First(ctx context.Context) (*GoodReward, error) {
	nodes, err := grq.Limit(1).All(setContextOp(ctx, grq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{goodreward.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (grq *GoodRewardQuery) FirstX(ctx context.Context) *GoodReward {
	node, err := grq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GoodReward ID from the query.
// Returns a *NotFoundError when no GoodReward ID was found.
func (grq *GoodRewardQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = grq.Limit(1).IDs(setContextOp(ctx, grq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{goodreward.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (grq *GoodRewardQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := grq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GoodReward entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GoodReward entity is found.
// Returns a *NotFoundError when no GoodReward entities are found.
func (grq *GoodRewardQuery) Only(ctx context.Context) (*GoodReward, error) {
	nodes, err := grq.Limit(2).All(setContextOp(ctx, grq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{goodreward.Label}
	default:
		return nil, &NotSingularError{goodreward.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (grq *GoodRewardQuery) OnlyX(ctx context.Context) *GoodReward {
	node, err := grq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GoodReward ID in the query.
// Returns a *NotSingularError when more than one GoodReward ID is found.
// Returns a *NotFoundError when no entities are found.
func (grq *GoodRewardQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = grq.Limit(2).IDs(setContextOp(ctx, grq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{goodreward.Label}
	default:
		err = &NotSingularError{goodreward.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (grq *GoodRewardQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := grq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GoodRewards.
func (grq *GoodRewardQuery) All(ctx context.Context) ([]*GoodReward, error) {
	ctx = setContextOp(ctx, grq.ctx, ent.OpQueryAll)
	if err := grq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GoodReward, *GoodRewardQuery]()
	return withInterceptors[[]*GoodReward](ctx, grq, qr, grq.inters)
}

// AllX is like All, but panics if an error occurs.
func (grq *GoodRewardQuery) AllX(ctx context.Context) []*GoodReward {
	nodes, err := grq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GoodReward IDs.
func (grq *GoodRewardQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if grq.ctx.Unique == nil && grq.path != nil {
		grq.Unique(true)
	}
	ctx = setContextOp(ctx, grq.ctx, ent.OpQueryIDs)
	if err = grq.Select(goodreward.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (grq *GoodRewardQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := grq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (grq *GoodRewardQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, grq.ctx, ent.OpQueryCount)
	if err := grq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, grq, querierCount[*GoodRewardQuery](), grq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (grq *GoodRewardQuery) CountX(ctx context.Context) int {
	count, err := grq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (grq *GoodRewardQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, grq.ctx, ent.OpQueryExist)
	switch _, err := grq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("generated: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (grq *GoodRewardQuery) ExistX(ctx context.Context) bool {
	exist, err := grq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GoodRewardQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (grq *GoodRewardQuery) Clone() *GoodRewardQuery {
	if grq == nil {
		return nil
	}
	return &GoodRewardQuery{
		config:     grq.config,
		ctx:        grq.ctx.Clone(),
		order:      append([]goodreward.OrderOption{}, grq.order...),
		inters:     append([]Interceptor{}, grq.inters...),
		predicates: append([]predicate.GoodReward{}, grq.predicates...),
		// clone intermediate query.
		sql:       grq.sql.Clone(),
		path:      grq.path,
		modifiers: append([]func(*sql.Selector){}, grq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GoodReward.Query().
//		GroupBy(goodreward.FieldEntID).
//		Aggregate(generated.Count()).
//		Scan(ctx, &v)
func (grq *GoodRewardQuery) GroupBy(field string, fields ...string) *GoodRewardGroupBy {
	grq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GoodRewardGroupBy{build: grq}
	grbuild.flds = &grq.ctx.Fields
	grbuild.label = goodreward.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//	}
//
//	client.GoodReward.Query().
//		Select(goodreward.FieldEntID).
//		Scan(ctx, &v)
func (grq *GoodRewardQuery) Select(fields ...string) *GoodRewardSelect {
	grq.ctx.Fields = append(grq.ctx.Fields, fields...)
	sbuild := &GoodRewardSelect{GoodRewardQuery: grq}
	sbuild.label = goodreward.Label
	sbuild.flds, sbuild.scan = &grq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GoodRewardSelect configured with the given aggregations.
func (grq *GoodRewardQuery) Aggregate(fns ...AggregateFunc) *GoodRewardSelect {
	return grq.Select().Aggregate(fns...)
}

func (grq *GoodRewardQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range grq.inters {
		if inter == nil {
			return fmt.Errorf("generated: uninitialized interceptor (forgotten import generated/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, grq); err != nil {
				return err
			}
		}
	}
	for _, f := range grq.ctx.Fields {
		if !goodreward.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("generated: invalid field %q for query", f)}
		}
	}
	if grq.path != nil {
		prev, err := grq.path(ctx)
		if err != nil {
			return err
		}
		grq.sql = prev
	}
	return nil
}

func (grq *GoodRewardQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GoodReward, error) {
	var (
		nodes = []*GoodReward{}
		_spec = grq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GoodReward).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GoodReward{config: grq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(grq.modifiers) > 0 {
		_spec.Modifiers = grq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, grq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (grq *GoodRewardQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := grq.querySpec()
	if len(grq.modifiers) > 0 {
		_spec.Modifiers = grq.modifiers
	}
	_spec.Node.Columns = grq.ctx.Fields
	if len(grq.ctx.Fields) > 0 {
		_spec.Unique = grq.ctx.Unique != nil && *grq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, grq.driver, _spec)
}

func (grq *GoodRewardQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(goodreward.Table, goodreward.Columns, sqlgraph.NewFieldSpec(goodreward.FieldID, field.TypeUint32))
	_spec.From = grq.sql
	if unique := grq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if grq.path != nil {
		_spec.Unique = true
	}
	if fields := grq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, goodreward.FieldID)
		for i := range fields {
			if fields[i] != goodreward.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := grq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := grq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := grq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := grq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (grq *GoodRewardQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(grq.driver.Dialect())
	t1 := builder.Table(goodreward.Table)
	columns := grq.ctx.Fields
	if len(columns) == 0 {
		columns = goodreward.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if grq.sql != nil {
		selector = grq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if grq.ctx.Unique != nil && *grq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range grq.modifiers {
		m(selector)
	}
	for _, p := range grq.predicates {
		p(selector)
	}
	for _, p := range grq.order {
		p(selector)
	}
	if offset := grq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := grq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (grq *GoodRewardQuery) ForUpdate(opts ...sql.LockOption) *GoodRewardQuery {
	if grq.driver.Dialect() == dialect.Postgres {
		grq.Unique(false)
	}
	grq.modifiers = append(grq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return grq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (grq *GoodRewardQuery) ForShare(opts ...sql.LockOption) *GoodRewardQuery {
	if grq.driver.Dialect() == dialect.Postgres {
		grq.Unique(false)
	}
	grq.modifiers = append(grq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return grq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (grq *GoodRewardQuery) Modify(modifiers ...func(s *sql.Selector)) *GoodRewardSelect {
	grq.modifiers = append(grq.modifiers, modifiers...)
	return grq.Select()
}

// GoodRewardGroupBy is the group-by builder for GoodReward entities.
type GoodRewardGroupBy struct {
	selector
	build *GoodRewardQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (grgb *GoodRewardGroupBy) Aggregate(fns ...AggregateFunc) *GoodRewardGroupBy {
	grgb.fns = append(grgb.fns, fns...)
	return grgb
}

// Scan applies the selector query and scans the result into the given value.
func (grgb *GoodRewardGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, grgb.build.ctx, ent.OpQueryGroupBy)
	if err := grgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GoodRewardQuery, *GoodRewardGroupBy](ctx, grgb.build, grgb, grgb.build.inters, v)
}

func (grgb *GoodRewardGroupBy) sqlScan(ctx context.Context, root *GoodRewardQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(grgb.fns))
	for _, fn := range grgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*grgb.flds)+len(grgb.fns))
		for _, f := range *grgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*grgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := grgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GoodRewardSelect is the builder for selecting fields of GoodReward entities.
type GoodRewardSelect struct {
	*GoodRewardQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (grs *GoodRewardSelect) Aggregate(fns ...AggregateFunc) *GoodRewardSelect {
	grs.fns = append(grs.fns, fns...)
	return grs
}

// Scan applies the selector query and scans the result into the given value.
func (grs *GoodRewardSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, grs.ctx, ent.OpQuerySelect)
	if err := grs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GoodRewardQuery, *GoodRewardSelect](ctx, grs.GoodRewardQuery, grs, grs.inters, v)
}

func (grs *GoodRewardSelect) sqlScan(ctx context.Context, root *GoodRewardQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(grs.fns))
	for _, fn := range grs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*grs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := grs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (grs *GoodRewardSelect) Modify(modifiers ...func(s *sql.Selector)) *GoodRewardSelect {
	grs.modifiers = append(grs.modifiers, modifiers...)
	return grs
}
