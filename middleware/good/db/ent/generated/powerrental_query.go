// Code generated by ent, DO NOT EDIT.

package generated

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/kunman/middleware/good/db/ent/generated/powerrental"
	"github.com/NpoolPlatform/kunman/middleware/good/db/ent/generated/predicate"
)

// PowerRentalQuery is the builder for querying PowerRental entities.
type PowerRentalQuery struct {
	config
	ctx        *QueryContext
	order      []powerrental.OrderOption
	inters     []Interceptor
	predicates []predicate.PowerRental
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PowerRentalQuery builder.
func (prq *PowerRentalQuery) Where(ps ...predicate.PowerRental) *PowerRentalQuery {
	prq.predicates = append(prq.predicates, ps...)
	return prq
}

// Limit the number of records to be returned by this query.
func (prq *PowerRentalQuery) Limit(limit int) *PowerRentalQuery {
	prq.ctx.Limit = &limit
	return prq
}

// Offset to start from.
func (prq *PowerRentalQuery) Offset(offset int) *PowerRentalQuery {
	prq.ctx.Offset = &offset
	return prq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (prq *PowerRentalQuery) Unique(unique bool) *PowerRentalQuery {
	prq.ctx.Unique = &unique
	return prq
}

// Order specifies how the records should be ordered.
func (prq *PowerRentalQuery) Order(o ...powerrental.OrderOption) *PowerRentalQuery {
	prq.order = append(prq.order, o...)
	return prq
}

// First returns the first PowerRental entity from the query.
// Returns a *NotFoundError when no PowerRental was found.
func (prq *PowerRentalQuery) First(ctx context.Context) (*PowerRental, error) {
	nodes, err := prq.Limit(1).All(setContextOp(ctx, prq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{powerrental.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (prq *PowerRentalQuery) FirstX(ctx context.Context) *PowerRental {
	node, err := prq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PowerRental ID from the query.
// Returns a *NotFoundError when no PowerRental ID was found.
func (prq *PowerRentalQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = prq.Limit(1).IDs(setContextOp(ctx, prq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{powerrental.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (prq *PowerRentalQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := prq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PowerRental entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PowerRental entity is found.
// Returns a *NotFoundError when no PowerRental entities are found.
func (prq *PowerRentalQuery) Only(ctx context.Context) (*PowerRental, error) {
	nodes, err := prq.Limit(2).All(setContextOp(ctx, prq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{powerrental.Label}
	default:
		return nil, &NotSingularError{powerrental.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (prq *PowerRentalQuery) OnlyX(ctx context.Context) *PowerRental {
	node, err := prq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PowerRental ID in the query.
// Returns a *NotSingularError when more than one PowerRental ID is found.
// Returns a *NotFoundError when no entities are found.
func (prq *PowerRentalQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = prq.Limit(2).IDs(setContextOp(ctx, prq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{powerrental.Label}
	default:
		err = &NotSingularError{powerrental.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (prq *PowerRentalQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := prq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PowerRentals.
func (prq *PowerRentalQuery) All(ctx context.Context) ([]*PowerRental, error) {
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryAll)
	if err := prq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PowerRental, *PowerRentalQuery]()
	return withInterceptors[[]*PowerRental](ctx, prq, qr, prq.inters)
}

// AllX is like All, but panics if an error occurs.
func (prq *PowerRentalQuery) AllX(ctx context.Context) []*PowerRental {
	nodes, err := prq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PowerRental IDs.
func (prq *PowerRentalQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if prq.ctx.Unique == nil && prq.path != nil {
		prq.Unique(true)
	}
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryIDs)
	if err = prq.Select(powerrental.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (prq *PowerRentalQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := prq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (prq *PowerRentalQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryCount)
	if err := prq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, prq, querierCount[*PowerRentalQuery](), prq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (prq *PowerRentalQuery) CountX(ctx context.Context) int {
	count, err := prq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (prq *PowerRentalQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryExist)
	switch _, err := prq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("generated: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (prq *PowerRentalQuery) ExistX(ctx context.Context) bool {
	exist, err := prq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PowerRentalQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (prq *PowerRentalQuery) Clone() *PowerRentalQuery {
	if prq == nil {
		return nil
	}
	return &PowerRentalQuery{
		config:     prq.config,
		ctx:        prq.ctx.Clone(),
		order:      append([]powerrental.OrderOption{}, prq.order...),
		inters:     append([]Interceptor{}, prq.inters...),
		predicates: append([]predicate.PowerRental{}, prq.predicates...),
		// clone intermediate query.
		sql:       prq.sql.Clone(),
		path:      prq.path,
		modifiers: append([]func(*sql.Selector){}, prq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PowerRental.Query().
//		GroupBy(powerrental.FieldEntID).
//		Aggregate(generated.Count()).
//		Scan(ctx, &v)
func (prq *PowerRentalQuery) GroupBy(field string, fields ...string) *PowerRentalGroupBy {
	prq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PowerRentalGroupBy{build: prq}
	grbuild.flds = &prq.ctx.Fields
	grbuild.label = powerrental.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//	}
//
//	client.PowerRental.Query().
//		Select(powerrental.FieldEntID).
//		Scan(ctx, &v)
func (prq *PowerRentalQuery) Select(fields ...string) *PowerRentalSelect {
	prq.ctx.Fields = append(prq.ctx.Fields, fields...)
	sbuild := &PowerRentalSelect{PowerRentalQuery: prq}
	sbuild.label = powerrental.Label
	sbuild.flds, sbuild.scan = &prq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PowerRentalSelect configured with the given aggregations.
func (prq *PowerRentalQuery) Aggregate(fns ...AggregateFunc) *PowerRentalSelect {
	return prq.Select().Aggregate(fns...)
}

func (prq *PowerRentalQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range prq.inters {
		if inter == nil {
			return fmt.Errorf("generated: uninitialized interceptor (forgotten import generated/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, prq); err != nil {
				return err
			}
		}
	}
	for _, f := range prq.ctx.Fields {
		if !powerrental.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("generated: invalid field %q for query", f)}
		}
	}
	if prq.path != nil {
		prev, err := prq.path(ctx)
		if err != nil {
			return err
		}
		prq.sql = prev
	}
	return nil
}

func (prq *PowerRentalQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PowerRental, error) {
	var (
		nodes = []*PowerRental{}
		_spec = prq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PowerRental).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PowerRental{config: prq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(prq.modifiers) > 0 {
		_spec.Modifiers = prq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, prq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (prq *PowerRentalQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := prq.querySpec()
	if len(prq.modifiers) > 0 {
		_spec.Modifiers = prq.modifiers
	}
	_spec.Node.Columns = prq.ctx.Fields
	if len(prq.ctx.Fields) > 0 {
		_spec.Unique = prq.ctx.Unique != nil && *prq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, prq.driver, _spec)
}

func (prq *PowerRentalQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(powerrental.Table, powerrental.Columns, sqlgraph.NewFieldSpec(powerrental.FieldID, field.TypeUint32))
	_spec.From = prq.sql
	if unique := prq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if prq.path != nil {
		_spec.Unique = true
	}
	if fields := prq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, powerrental.FieldID)
		for i := range fields {
			if fields[i] != powerrental.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := prq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := prq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := prq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := prq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (prq *PowerRentalQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(prq.driver.Dialect())
	t1 := builder.Table(powerrental.Table)
	columns := prq.ctx.Fields
	if len(columns) == 0 {
		columns = powerrental.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if prq.sql != nil {
		selector = prq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if prq.ctx.Unique != nil && *prq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range prq.modifiers {
		m(selector)
	}
	for _, p := range prq.predicates {
		p(selector)
	}
	for _, p := range prq.order {
		p(selector)
	}
	if offset := prq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := prq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (prq *PowerRentalQuery) ForUpdate(opts ...sql.LockOption) *PowerRentalQuery {
	if prq.driver.Dialect() == dialect.Postgres {
		prq.Unique(false)
	}
	prq.modifiers = append(prq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return prq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (prq *PowerRentalQuery) ForShare(opts ...sql.LockOption) *PowerRentalQuery {
	if prq.driver.Dialect() == dialect.Postgres {
		prq.Unique(false)
	}
	prq.modifiers = append(prq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return prq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (prq *PowerRentalQuery) Modify(modifiers ...func(s *sql.Selector)) *PowerRentalSelect {
	prq.modifiers = append(prq.modifiers, modifiers...)
	return prq.Select()
}

// PowerRentalGroupBy is the group-by builder for PowerRental entities.
type PowerRentalGroupBy struct {
	selector
	build *PowerRentalQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (prgb *PowerRentalGroupBy) Aggregate(fns ...AggregateFunc) *PowerRentalGroupBy {
	prgb.fns = append(prgb.fns, fns...)
	return prgb
}

// Scan applies the selector query and scans the result into the given value.
func (prgb *PowerRentalGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prgb.build.ctx, ent.OpQueryGroupBy)
	if err := prgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PowerRentalQuery, *PowerRentalGroupBy](ctx, prgb.build, prgb, prgb.build.inters, v)
}

func (prgb *PowerRentalGroupBy) sqlScan(ctx context.Context, root *PowerRentalQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(prgb.fns))
	for _, fn := range prgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*prgb.flds)+len(prgb.fns))
		for _, f := range *prgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*prgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PowerRentalSelect is the builder for selecting fields of PowerRental entities.
type PowerRentalSelect struct {
	*PowerRentalQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (prs *PowerRentalSelect) Aggregate(fns ...AggregateFunc) *PowerRentalSelect {
	prs.fns = append(prs.fns, fns...)
	return prs
}

// Scan applies the selector query and scans the result into the given value.
func (prs *PowerRentalSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prs.ctx, ent.OpQuerySelect)
	if err := prs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PowerRentalQuery, *PowerRentalSelect](ctx, prs.PowerRentalQuery, prs, prs.inters, v)
}

func (prs *PowerRentalSelect) sqlScan(ctx context.Context, root *PowerRentalQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(prs.fns))
	for _, fn := range prs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*prs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (prs *PowerRentalSelect) Modify(modifiers ...func(s *sql.Selector)) *PowerRentalSelect {
	prs.modifiers = append(prs.modifiers, modifiers...)
	return prs
}
