// Code generated by ent, DO NOT EDIT.

package generated

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/kunman/middleware/good/db/ent/generated/goodcoin"
	"github.com/NpoolPlatform/kunman/middleware/good/db/ent/generated/predicate"
)

// GoodCoinQuery is the builder for querying GoodCoin entities.
type GoodCoinQuery struct {
	config
	ctx        *QueryContext
	order      []goodcoin.OrderOption
	inters     []Interceptor
	predicates []predicate.GoodCoin
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GoodCoinQuery builder.
func (gcq *GoodCoinQuery) Where(ps ...predicate.GoodCoin) *GoodCoinQuery {
	gcq.predicates = append(gcq.predicates, ps...)
	return gcq
}

// Limit the number of records to be returned by this query.
func (gcq *GoodCoinQuery) Limit(limit int) *GoodCoinQuery {
	gcq.ctx.Limit = &limit
	return gcq
}

// Offset to start from.
func (gcq *GoodCoinQuery) Offset(offset int) *GoodCoinQuery {
	gcq.ctx.Offset = &offset
	return gcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gcq *GoodCoinQuery) Unique(unique bool) *GoodCoinQuery {
	gcq.ctx.Unique = &unique
	return gcq
}

// Order specifies how the records should be ordered.
func (gcq *GoodCoinQuery) Order(o ...goodcoin.OrderOption) *GoodCoinQuery {
	gcq.order = append(gcq.order, o...)
	return gcq
}

// First returns the first GoodCoin entity from the query.
// Returns a *NotFoundError when no GoodCoin was found.
func (gcq *GoodCoinQuery) First(ctx context.Context) (*GoodCoin, error) {
	nodes, err := gcq.Limit(1).All(setContextOp(ctx, gcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{goodcoin.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gcq *GoodCoinQuery) FirstX(ctx context.Context) *GoodCoin {
	node, err := gcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GoodCoin ID from the query.
// Returns a *NotFoundError when no GoodCoin ID was found.
func (gcq *GoodCoinQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = gcq.Limit(1).IDs(setContextOp(ctx, gcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{goodcoin.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gcq *GoodCoinQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := gcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GoodCoin entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GoodCoin entity is found.
// Returns a *NotFoundError when no GoodCoin entities are found.
func (gcq *GoodCoinQuery) Only(ctx context.Context) (*GoodCoin, error) {
	nodes, err := gcq.Limit(2).All(setContextOp(ctx, gcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{goodcoin.Label}
	default:
		return nil, &NotSingularError{goodcoin.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gcq *GoodCoinQuery) OnlyX(ctx context.Context) *GoodCoin {
	node, err := gcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GoodCoin ID in the query.
// Returns a *NotSingularError when more than one GoodCoin ID is found.
// Returns a *NotFoundError when no entities are found.
func (gcq *GoodCoinQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = gcq.Limit(2).IDs(setContextOp(ctx, gcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{goodcoin.Label}
	default:
		err = &NotSingularError{goodcoin.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gcq *GoodCoinQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := gcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GoodCoins.
func (gcq *GoodCoinQuery) All(ctx context.Context) ([]*GoodCoin, error) {
	ctx = setContextOp(ctx, gcq.ctx, ent.OpQueryAll)
	if err := gcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GoodCoin, *GoodCoinQuery]()
	return withInterceptors[[]*GoodCoin](ctx, gcq, qr, gcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (gcq *GoodCoinQuery) AllX(ctx context.Context) []*GoodCoin {
	nodes, err := gcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GoodCoin IDs.
func (gcq *GoodCoinQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if gcq.ctx.Unique == nil && gcq.path != nil {
		gcq.Unique(true)
	}
	ctx = setContextOp(ctx, gcq.ctx, ent.OpQueryIDs)
	if err = gcq.Select(goodcoin.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gcq *GoodCoinQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := gcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gcq *GoodCoinQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, gcq.ctx, ent.OpQueryCount)
	if err := gcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, gcq, querierCount[*GoodCoinQuery](), gcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (gcq *GoodCoinQuery) CountX(ctx context.Context) int {
	count, err := gcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gcq *GoodCoinQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, gcq.ctx, ent.OpQueryExist)
	switch _, err := gcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("generated: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (gcq *GoodCoinQuery) ExistX(ctx context.Context) bool {
	exist, err := gcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GoodCoinQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gcq *GoodCoinQuery) Clone() *GoodCoinQuery {
	if gcq == nil {
		return nil
	}
	return &GoodCoinQuery{
		config:     gcq.config,
		ctx:        gcq.ctx.Clone(),
		order:      append([]goodcoin.OrderOption{}, gcq.order...),
		inters:     append([]Interceptor{}, gcq.inters...),
		predicates: append([]predicate.GoodCoin{}, gcq.predicates...),
		// clone intermediate query.
		sql:       gcq.sql.Clone(),
		path:      gcq.path,
		modifiers: append([]func(*sql.Selector){}, gcq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GoodCoin.Query().
//		GroupBy(goodcoin.FieldEntID).
//		Aggregate(generated.Count()).
//		Scan(ctx, &v)
func (gcq *GoodCoinQuery) GroupBy(field string, fields ...string) *GoodCoinGroupBy {
	gcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GoodCoinGroupBy{build: gcq}
	grbuild.flds = &gcq.ctx.Fields
	grbuild.label = goodcoin.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//	}
//
//	client.GoodCoin.Query().
//		Select(goodcoin.FieldEntID).
//		Scan(ctx, &v)
func (gcq *GoodCoinQuery) Select(fields ...string) *GoodCoinSelect {
	gcq.ctx.Fields = append(gcq.ctx.Fields, fields...)
	sbuild := &GoodCoinSelect{GoodCoinQuery: gcq}
	sbuild.label = goodcoin.Label
	sbuild.flds, sbuild.scan = &gcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GoodCoinSelect configured with the given aggregations.
func (gcq *GoodCoinQuery) Aggregate(fns ...AggregateFunc) *GoodCoinSelect {
	return gcq.Select().Aggregate(fns...)
}

func (gcq *GoodCoinQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range gcq.inters {
		if inter == nil {
			return fmt.Errorf("generated: uninitialized interceptor (forgotten import generated/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, gcq); err != nil {
				return err
			}
		}
	}
	for _, f := range gcq.ctx.Fields {
		if !goodcoin.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("generated: invalid field %q for query", f)}
		}
	}
	if gcq.path != nil {
		prev, err := gcq.path(ctx)
		if err != nil {
			return err
		}
		gcq.sql = prev
	}
	return nil
}

func (gcq *GoodCoinQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GoodCoin, error) {
	var (
		nodes = []*GoodCoin{}
		_spec = gcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GoodCoin).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GoodCoin{config: gcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(gcq.modifiers) > 0 {
		_spec.Modifiers = gcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, gcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (gcq *GoodCoinQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gcq.querySpec()
	if len(gcq.modifiers) > 0 {
		_spec.Modifiers = gcq.modifiers
	}
	_spec.Node.Columns = gcq.ctx.Fields
	if len(gcq.ctx.Fields) > 0 {
		_spec.Unique = gcq.ctx.Unique != nil && *gcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, gcq.driver, _spec)
}

func (gcq *GoodCoinQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(goodcoin.Table, goodcoin.Columns, sqlgraph.NewFieldSpec(goodcoin.FieldID, field.TypeUint32))
	_spec.From = gcq.sql
	if unique := gcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if gcq.path != nil {
		_spec.Unique = true
	}
	if fields := gcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, goodcoin.FieldID)
		for i := range fields {
			if fields[i] != goodcoin.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gcq *GoodCoinQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gcq.driver.Dialect())
	t1 := builder.Table(goodcoin.Table)
	columns := gcq.ctx.Fields
	if len(columns) == 0 {
		columns = goodcoin.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gcq.sql != nil {
		selector = gcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if gcq.ctx.Unique != nil && *gcq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range gcq.modifiers {
		m(selector)
	}
	for _, p := range gcq.predicates {
		p(selector)
	}
	for _, p := range gcq.order {
		p(selector)
	}
	if offset := gcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (gcq *GoodCoinQuery) ForUpdate(opts ...sql.LockOption) *GoodCoinQuery {
	if gcq.driver.Dialect() == dialect.Postgres {
		gcq.Unique(false)
	}
	gcq.modifiers = append(gcq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return gcq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (gcq *GoodCoinQuery) ForShare(opts ...sql.LockOption) *GoodCoinQuery {
	if gcq.driver.Dialect() == dialect.Postgres {
		gcq.Unique(false)
	}
	gcq.modifiers = append(gcq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return gcq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (gcq *GoodCoinQuery) Modify(modifiers ...func(s *sql.Selector)) *GoodCoinSelect {
	gcq.modifiers = append(gcq.modifiers, modifiers...)
	return gcq.Select()
}

// GoodCoinGroupBy is the group-by builder for GoodCoin entities.
type GoodCoinGroupBy struct {
	selector
	build *GoodCoinQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gcgb *GoodCoinGroupBy) Aggregate(fns ...AggregateFunc) *GoodCoinGroupBy {
	gcgb.fns = append(gcgb.fns, fns...)
	return gcgb
}

// Scan applies the selector query and scans the result into the given value.
func (gcgb *GoodCoinGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gcgb.build.ctx, ent.OpQueryGroupBy)
	if err := gcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GoodCoinQuery, *GoodCoinGroupBy](ctx, gcgb.build, gcgb, gcgb.build.inters, v)
}

func (gcgb *GoodCoinGroupBy) sqlScan(ctx context.Context, root *GoodCoinQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(gcgb.fns))
	for _, fn := range gcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*gcgb.flds)+len(gcgb.fns))
		for _, f := range *gcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*gcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GoodCoinSelect is the builder for selecting fields of GoodCoin entities.
type GoodCoinSelect struct {
	*GoodCoinQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (gcs *GoodCoinSelect) Aggregate(fns ...AggregateFunc) *GoodCoinSelect {
	gcs.fns = append(gcs.fns, fns...)
	return gcs
}

// Scan applies the selector query and scans the result into the given value.
func (gcs *GoodCoinSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gcs.ctx, ent.OpQuerySelect)
	if err := gcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GoodCoinQuery, *GoodCoinSelect](ctx, gcs.GoodCoinQuery, gcs, gcs.inters, v)
}

func (gcs *GoodCoinSelect) sqlScan(ctx context.Context, root *GoodCoinQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(gcs.fns))
	for _, fn := range gcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*gcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (gcs *GoodCoinSelect) Modify(modifiers ...func(s *sql.Selector)) *GoodCoinSelect {
	gcs.modifiers = append(gcs.modifiers, modifiers...)
	return gcs
}
