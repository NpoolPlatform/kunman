// Code generated by ent, DO NOT EDIT.

package generated

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/kunman/middleware/good/db/ent/generated/extrainfo"
	"github.com/NpoolPlatform/kunman/middleware/good/db/ent/generated/predicate"
)

// ExtraInfoQuery is the builder for querying ExtraInfo entities.
type ExtraInfoQuery struct {
	config
	ctx        *QueryContext
	order      []extrainfo.OrderOption
	inters     []Interceptor
	predicates []predicate.ExtraInfo
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ExtraInfoQuery builder.
func (eiq *ExtraInfoQuery) Where(ps ...predicate.ExtraInfo) *ExtraInfoQuery {
	eiq.predicates = append(eiq.predicates, ps...)
	return eiq
}

// Limit the number of records to be returned by this query.
func (eiq *ExtraInfoQuery) Limit(limit int) *ExtraInfoQuery {
	eiq.ctx.Limit = &limit
	return eiq
}

// Offset to start from.
func (eiq *ExtraInfoQuery) Offset(offset int) *ExtraInfoQuery {
	eiq.ctx.Offset = &offset
	return eiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eiq *ExtraInfoQuery) Unique(unique bool) *ExtraInfoQuery {
	eiq.ctx.Unique = &unique
	return eiq
}

// Order specifies how the records should be ordered.
func (eiq *ExtraInfoQuery) Order(o ...extrainfo.OrderOption) *ExtraInfoQuery {
	eiq.order = append(eiq.order, o...)
	return eiq
}

// First returns the first ExtraInfo entity from the query.
// Returns a *NotFoundError when no ExtraInfo was found.
func (eiq *ExtraInfoQuery) First(ctx context.Context) (*ExtraInfo, error) {
	nodes, err := eiq.Limit(1).All(setContextOp(ctx, eiq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{extrainfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eiq *ExtraInfoQuery) FirstX(ctx context.Context) *ExtraInfo {
	node, err := eiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ExtraInfo ID from the query.
// Returns a *NotFoundError when no ExtraInfo ID was found.
func (eiq *ExtraInfoQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = eiq.Limit(1).IDs(setContextOp(ctx, eiq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{extrainfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (eiq *ExtraInfoQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := eiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ExtraInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ExtraInfo entity is found.
// Returns a *NotFoundError when no ExtraInfo entities are found.
func (eiq *ExtraInfoQuery) Only(ctx context.Context) (*ExtraInfo, error) {
	nodes, err := eiq.Limit(2).All(setContextOp(ctx, eiq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{extrainfo.Label}
	default:
		return nil, &NotSingularError{extrainfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eiq *ExtraInfoQuery) OnlyX(ctx context.Context) *ExtraInfo {
	node, err := eiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ExtraInfo ID in the query.
// Returns a *NotSingularError when more than one ExtraInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (eiq *ExtraInfoQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = eiq.Limit(2).IDs(setContextOp(ctx, eiq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{extrainfo.Label}
	default:
		err = &NotSingularError{extrainfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (eiq *ExtraInfoQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := eiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ExtraInfos.
func (eiq *ExtraInfoQuery) All(ctx context.Context) ([]*ExtraInfo, error) {
	ctx = setContextOp(ctx, eiq.ctx, ent.OpQueryAll)
	if err := eiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ExtraInfo, *ExtraInfoQuery]()
	return withInterceptors[[]*ExtraInfo](ctx, eiq, qr, eiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (eiq *ExtraInfoQuery) AllX(ctx context.Context) []*ExtraInfo {
	nodes, err := eiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ExtraInfo IDs.
func (eiq *ExtraInfoQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if eiq.ctx.Unique == nil && eiq.path != nil {
		eiq.Unique(true)
	}
	ctx = setContextOp(ctx, eiq.ctx, ent.OpQueryIDs)
	if err = eiq.Select(extrainfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (eiq *ExtraInfoQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := eiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (eiq *ExtraInfoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, eiq.ctx, ent.OpQueryCount)
	if err := eiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, eiq, querierCount[*ExtraInfoQuery](), eiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (eiq *ExtraInfoQuery) CountX(ctx context.Context) int {
	count, err := eiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eiq *ExtraInfoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, eiq.ctx, ent.OpQueryExist)
	switch _, err := eiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("generated: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (eiq *ExtraInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := eiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ExtraInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eiq *ExtraInfoQuery) Clone() *ExtraInfoQuery {
	if eiq == nil {
		return nil
	}
	return &ExtraInfoQuery{
		config:     eiq.config,
		ctx:        eiq.ctx.Clone(),
		order:      append([]extrainfo.OrderOption{}, eiq.order...),
		inters:     append([]Interceptor{}, eiq.inters...),
		predicates: append([]predicate.ExtraInfo{}, eiq.predicates...),
		// clone intermediate query.
		sql:       eiq.sql.Clone(),
		path:      eiq.path,
		modifiers: append([]func(*sql.Selector){}, eiq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ExtraInfo.Query().
//		GroupBy(extrainfo.FieldEntID).
//		Aggregate(generated.Count()).
//		Scan(ctx, &v)
func (eiq *ExtraInfoQuery) GroupBy(field string, fields ...string) *ExtraInfoGroupBy {
	eiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ExtraInfoGroupBy{build: eiq}
	grbuild.flds = &eiq.ctx.Fields
	grbuild.label = extrainfo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//	}
//
//	client.ExtraInfo.Query().
//		Select(extrainfo.FieldEntID).
//		Scan(ctx, &v)
func (eiq *ExtraInfoQuery) Select(fields ...string) *ExtraInfoSelect {
	eiq.ctx.Fields = append(eiq.ctx.Fields, fields...)
	sbuild := &ExtraInfoSelect{ExtraInfoQuery: eiq}
	sbuild.label = extrainfo.Label
	sbuild.flds, sbuild.scan = &eiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ExtraInfoSelect configured with the given aggregations.
func (eiq *ExtraInfoQuery) Aggregate(fns ...AggregateFunc) *ExtraInfoSelect {
	return eiq.Select().Aggregate(fns...)
}

func (eiq *ExtraInfoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range eiq.inters {
		if inter == nil {
			return fmt.Errorf("generated: uninitialized interceptor (forgotten import generated/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, eiq); err != nil {
				return err
			}
		}
	}
	for _, f := range eiq.ctx.Fields {
		if !extrainfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("generated: invalid field %q for query", f)}
		}
	}
	if eiq.path != nil {
		prev, err := eiq.path(ctx)
		if err != nil {
			return err
		}
		eiq.sql = prev
	}
	return nil
}

func (eiq *ExtraInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ExtraInfo, error) {
	var (
		nodes = []*ExtraInfo{}
		_spec = eiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ExtraInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ExtraInfo{config: eiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(eiq.modifiers) > 0 {
		_spec.Modifiers = eiq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (eiq *ExtraInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eiq.querySpec()
	if len(eiq.modifiers) > 0 {
		_spec.Modifiers = eiq.modifiers
	}
	_spec.Node.Columns = eiq.ctx.Fields
	if len(eiq.ctx.Fields) > 0 {
		_spec.Unique = eiq.ctx.Unique != nil && *eiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, eiq.driver, _spec)
}

func (eiq *ExtraInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(extrainfo.Table, extrainfo.Columns, sqlgraph.NewFieldSpec(extrainfo.FieldID, field.TypeUint32))
	_spec.From = eiq.sql
	if unique := eiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if eiq.path != nil {
		_spec.Unique = true
	}
	if fields := eiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, extrainfo.FieldID)
		for i := range fields {
			if fields[i] != extrainfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := eiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eiq *ExtraInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eiq.driver.Dialect())
	t1 := builder.Table(extrainfo.Table)
	columns := eiq.ctx.Fields
	if len(columns) == 0 {
		columns = extrainfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eiq.sql != nil {
		selector = eiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eiq.ctx.Unique != nil && *eiq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range eiq.modifiers {
		m(selector)
	}
	for _, p := range eiq.predicates {
		p(selector)
	}
	for _, p := range eiq.order {
		p(selector)
	}
	if offset := eiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (eiq *ExtraInfoQuery) ForUpdate(opts ...sql.LockOption) *ExtraInfoQuery {
	if eiq.driver.Dialect() == dialect.Postgres {
		eiq.Unique(false)
	}
	eiq.modifiers = append(eiq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return eiq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (eiq *ExtraInfoQuery) ForShare(opts ...sql.LockOption) *ExtraInfoQuery {
	if eiq.driver.Dialect() == dialect.Postgres {
		eiq.Unique(false)
	}
	eiq.modifiers = append(eiq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return eiq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (eiq *ExtraInfoQuery) Modify(modifiers ...func(s *sql.Selector)) *ExtraInfoSelect {
	eiq.modifiers = append(eiq.modifiers, modifiers...)
	return eiq.Select()
}

// ExtraInfoGroupBy is the group-by builder for ExtraInfo entities.
type ExtraInfoGroupBy struct {
	selector
	build *ExtraInfoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (eigb *ExtraInfoGroupBy) Aggregate(fns ...AggregateFunc) *ExtraInfoGroupBy {
	eigb.fns = append(eigb.fns, fns...)
	return eigb
}

// Scan applies the selector query and scans the result into the given value.
func (eigb *ExtraInfoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eigb.build.ctx, ent.OpQueryGroupBy)
	if err := eigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExtraInfoQuery, *ExtraInfoGroupBy](ctx, eigb.build, eigb, eigb.build.inters, v)
}

func (eigb *ExtraInfoGroupBy) sqlScan(ctx context.Context, root *ExtraInfoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(eigb.fns))
	for _, fn := range eigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*eigb.flds)+len(eigb.fns))
		for _, f := range *eigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*eigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ExtraInfoSelect is the builder for selecting fields of ExtraInfo entities.
type ExtraInfoSelect struct {
	*ExtraInfoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (eis *ExtraInfoSelect) Aggregate(fns ...AggregateFunc) *ExtraInfoSelect {
	eis.fns = append(eis.fns, fns...)
	return eis
}

// Scan applies the selector query and scans the result into the given value.
func (eis *ExtraInfoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eis.ctx, ent.OpQuerySelect)
	if err := eis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExtraInfoQuery, *ExtraInfoSelect](ctx, eis.ExtraInfoQuery, eis, eis.inters, v)
}

func (eis *ExtraInfoSelect) sqlScan(ctx context.Context, root *ExtraInfoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(eis.fns))
	for _, fn := range eis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*eis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (eis *ExtraInfoSelect) Modify(modifiers ...func(s *sql.Selector)) *ExtraInfoSelect {
	eis.modifiers = append(eis.modifiers, modifiers...)
	return eis
}
