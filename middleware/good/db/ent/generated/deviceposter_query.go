// Code generated by ent, DO NOT EDIT.

package generated

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/kunman/middleware/good/db/ent/generated/deviceposter"
	"github.com/NpoolPlatform/kunman/middleware/good/db/ent/generated/predicate"
)

// DevicePosterQuery is the builder for querying DevicePoster entities.
type DevicePosterQuery struct {
	config
	ctx        *QueryContext
	order      []deviceposter.OrderOption
	inters     []Interceptor
	predicates []predicate.DevicePoster
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DevicePosterQuery builder.
func (dpq *DevicePosterQuery) Where(ps ...predicate.DevicePoster) *DevicePosterQuery {
	dpq.predicates = append(dpq.predicates, ps...)
	return dpq
}

// Limit the number of records to be returned by this query.
func (dpq *DevicePosterQuery) Limit(limit int) *DevicePosterQuery {
	dpq.ctx.Limit = &limit
	return dpq
}

// Offset to start from.
func (dpq *DevicePosterQuery) Offset(offset int) *DevicePosterQuery {
	dpq.ctx.Offset = &offset
	return dpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dpq *DevicePosterQuery) Unique(unique bool) *DevicePosterQuery {
	dpq.ctx.Unique = &unique
	return dpq
}

// Order specifies how the records should be ordered.
func (dpq *DevicePosterQuery) Order(o ...deviceposter.OrderOption) *DevicePosterQuery {
	dpq.order = append(dpq.order, o...)
	return dpq
}

// First returns the first DevicePoster entity from the query.
// Returns a *NotFoundError when no DevicePoster was found.
func (dpq *DevicePosterQuery) First(ctx context.Context) (*DevicePoster, error) {
	nodes, err := dpq.Limit(1).All(setContextOp(ctx, dpq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{deviceposter.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dpq *DevicePosterQuery) FirstX(ctx context.Context) *DevicePoster {
	node, err := dpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DevicePoster ID from the query.
// Returns a *NotFoundError when no DevicePoster ID was found.
func (dpq *DevicePosterQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = dpq.Limit(1).IDs(setContextOp(ctx, dpq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{deviceposter.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dpq *DevicePosterQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := dpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DevicePoster entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DevicePoster entity is found.
// Returns a *NotFoundError when no DevicePoster entities are found.
func (dpq *DevicePosterQuery) Only(ctx context.Context) (*DevicePoster, error) {
	nodes, err := dpq.Limit(2).All(setContextOp(ctx, dpq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{deviceposter.Label}
	default:
		return nil, &NotSingularError{deviceposter.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dpq *DevicePosterQuery) OnlyX(ctx context.Context) *DevicePoster {
	node, err := dpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DevicePoster ID in the query.
// Returns a *NotSingularError when more than one DevicePoster ID is found.
// Returns a *NotFoundError when no entities are found.
func (dpq *DevicePosterQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = dpq.Limit(2).IDs(setContextOp(ctx, dpq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{deviceposter.Label}
	default:
		err = &NotSingularError{deviceposter.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dpq *DevicePosterQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := dpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DevicePosters.
func (dpq *DevicePosterQuery) All(ctx context.Context) ([]*DevicePoster, error) {
	ctx = setContextOp(ctx, dpq.ctx, ent.OpQueryAll)
	if err := dpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DevicePoster, *DevicePosterQuery]()
	return withInterceptors[[]*DevicePoster](ctx, dpq, qr, dpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dpq *DevicePosterQuery) AllX(ctx context.Context) []*DevicePoster {
	nodes, err := dpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DevicePoster IDs.
func (dpq *DevicePosterQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if dpq.ctx.Unique == nil && dpq.path != nil {
		dpq.Unique(true)
	}
	ctx = setContextOp(ctx, dpq.ctx, ent.OpQueryIDs)
	if err = dpq.Select(deviceposter.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dpq *DevicePosterQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := dpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dpq *DevicePosterQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dpq.ctx, ent.OpQueryCount)
	if err := dpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dpq, querierCount[*DevicePosterQuery](), dpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dpq *DevicePosterQuery) CountX(ctx context.Context) int {
	count, err := dpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dpq *DevicePosterQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dpq.ctx, ent.OpQueryExist)
	switch _, err := dpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("generated: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dpq *DevicePosterQuery) ExistX(ctx context.Context) bool {
	exist, err := dpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DevicePosterQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dpq *DevicePosterQuery) Clone() *DevicePosterQuery {
	if dpq == nil {
		return nil
	}
	return &DevicePosterQuery{
		config:     dpq.config,
		ctx:        dpq.ctx.Clone(),
		order:      append([]deviceposter.OrderOption{}, dpq.order...),
		inters:     append([]Interceptor{}, dpq.inters...),
		predicates: append([]predicate.DevicePoster{}, dpq.predicates...),
		// clone intermediate query.
		sql:       dpq.sql.Clone(),
		path:      dpq.path,
		modifiers: append([]func(*sql.Selector){}, dpq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DevicePoster.Query().
//		GroupBy(deviceposter.FieldEntID).
//		Aggregate(generated.Count()).
//		Scan(ctx, &v)
func (dpq *DevicePosterQuery) GroupBy(field string, fields ...string) *DevicePosterGroupBy {
	dpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DevicePosterGroupBy{build: dpq}
	grbuild.flds = &dpq.ctx.Fields
	grbuild.label = deviceposter.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntID uuid.UUID `json:"ent_id,omitempty"`
//	}
//
//	client.DevicePoster.Query().
//		Select(deviceposter.FieldEntID).
//		Scan(ctx, &v)
func (dpq *DevicePosterQuery) Select(fields ...string) *DevicePosterSelect {
	dpq.ctx.Fields = append(dpq.ctx.Fields, fields...)
	sbuild := &DevicePosterSelect{DevicePosterQuery: dpq}
	sbuild.label = deviceposter.Label
	sbuild.flds, sbuild.scan = &dpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DevicePosterSelect configured with the given aggregations.
func (dpq *DevicePosterQuery) Aggregate(fns ...AggregateFunc) *DevicePosterSelect {
	return dpq.Select().Aggregate(fns...)
}

func (dpq *DevicePosterQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dpq.inters {
		if inter == nil {
			return fmt.Errorf("generated: uninitialized interceptor (forgotten import generated/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dpq); err != nil {
				return err
			}
		}
	}
	for _, f := range dpq.ctx.Fields {
		if !deviceposter.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("generated: invalid field %q for query", f)}
		}
	}
	if dpq.path != nil {
		prev, err := dpq.path(ctx)
		if err != nil {
			return err
		}
		dpq.sql = prev
	}
	return nil
}

func (dpq *DevicePosterQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DevicePoster, error) {
	var (
		nodes = []*DevicePoster{}
		_spec = dpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DevicePoster).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DevicePoster{config: dpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(dpq.modifiers) > 0 {
		_spec.Modifiers = dpq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (dpq *DevicePosterQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dpq.querySpec()
	if len(dpq.modifiers) > 0 {
		_spec.Modifiers = dpq.modifiers
	}
	_spec.Node.Columns = dpq.ctx.Fields
	if len(dpq.ctx.Fields) > 0 {
		_spec.Unique = dpq.ctx.Unique != nil && *dpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dpq.driver, _spec)
}

func (dpq *DevicePosterQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(deviceposter.Table, deviceposter.Columns, sqlgraph.NewFieldSpec(deviceposter.FieldID, field.TypeUint32))
	_spec.From = dpq.sql
	if unique := dpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dpq.path != nil {
		_spec.Unique = true
	}
	if fields := dpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, deviceposter.FieldID)
		for i := range fields {
			if fields[i] != deviceposter.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := dpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dpq *DevicePosterQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dpq.driver.Dialect())
	t1 := builder.Table(deviceposter.Table)
	columns := dpq.ctx.Fields
	if len(columns) == 0 {
		columns = deviceposter.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dpq.sql != nil {
		selector = dpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dpq.ctx.Unique != nil && *dpq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range dpq.modifiers {
		m(selector)
	}
	for _, p := range dpq.predicates {
		p(selector)
	}
	for _, p := range dpq.order {
		p(selector)
	}
	if offset := dpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (dpq *DevicePosterQuery) ForUpdate(opts ...sql.LockOption) *DevicePosterQuery {
	if dpq.driver.Dialect() == dialect.Postgres {
		dpq.Unique(false)
	}
	dpq.modifiers = append(dpq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return dpq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (dpq *DevicePosterQuery) ForShare(opts ...sql.LockOption) *DevicePosterQuery {
	if dpq.driver.Dialect() == dialect.Postgres {
		dpq.Unique(false)
	}
	dpq.modifiers = append(dpq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return dpq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (dpq *DevicePosterQuery) Modify(modifiers ...func(s *sql.Selector)) *DevicePosterSelect {
	dpq.modifiers = append(dpq.modifiers, modifiers...)
	return dpq.Select()
}

// DevicePosterGroupBy is the group-by builder for DevicePoster entities.
type DevicePosterGroupBy struct {
	selector
	build *DevicePosterQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dpgb *DevicePosterGroupBy) Aggregate(fns ...AggregateFunc) *DevicePosterGroupBy {
	dpgb.fns = append(dpgb.fns, fns...)
	return dpgb
}

// Scan applies the selector query and scans the result into the given value.
func (dpgb *DevicePosterGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dpgb.build.ctx, ent.OpQueryGroupBy)
	if err := dpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DevicePosterQuery, *DevicePosterGroupBy](ctx, dpgb.build, dpgb, dpgb.build.inters, v)
}

func (dpgb *DevicePosterGroupBy) sqlScan(ctx context.Context, root *DevicePosterQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dpgb.fns))
	for _, fn := range dpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dpgb.flds)+len(dpgb.fns))
		for _, f := range *dpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DevicePosterSelect is the builder for selecting fields of DevicePoster entities.
type DevicePosterSelect struct {
	*DevicePosterQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dps *DevicePosterSelect) Aggregate(fns ...AggregateFunc) *DevicePosterSelect {
	dps.fns = append(dps.fns, fns...)
	return dps
}

// Scan applies the selector query and scans the result into the given value.
func (dps *DevicePosterSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dps.ctx, ent.OpQuerySelect)
	if err := dps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DevicePosterQuery, *DevicePosterSelect](ctx, dps.DevicePosterQuery, dps, dps.inters, v)
}

func (dps *DevicePosterSelect) sqlScan(ctx context.Context, root *DevicePosterQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dps.fns))
	for _, fn := range dps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (dps *DevicePosterSelect) Modify(modifiers ...func(s *sql.Selector)) *DevicePosterSelect {
	dps.modifiers = append(dps.modifiers, modifiers...)
	return dps
}
