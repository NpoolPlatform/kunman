// Code generated by ent, DO NOT EDIT.

package generated

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/kunman/middleware/chain/db/ent/generated/currencyfeed"
	"github.com/NpoolPlatform/kunman/middleware/chain/db/ent/generated/predicate"
)

// CurrencyFeedQuery is the builder for querying CurrencyFeed entities.
type CurrencyFeedQuery struct {
	config
	ctx        *QueryContext
	order      []currencyfeed.OrderOption
	inters     []Interceptor
	predicates []predicate.CurrencyFeed
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CurrencyFeedQuery builder.
func (cfq *CurrencyFeedQuery) Where(ps ...predicate.CurrencyFeed) *CurrencyFeedQuery {
	cfq.predicates = append(cfq.predicates, ps...)
	return cfq
}

// Limit the number of records to be returned by this query.
func (cfq *CurrencyFeedQuery) Limit(limit int) *CurrencyFeedQuery {
	cfq.ctx.Limit = &limit
	return cfq
}

// Offset to start from.
func (cfq *CurrencyFeedQuery) Offset(offset int) *CurrencyFeedQuery {
	cfq.ctx.Offset = &offset
	return cfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cfq *CurrencyFeedQuery) Unique(unique bool) *CurrencyFeedQuery {
	cfq.ctx.Unique = &unique
	return cfq
}

// Order specifies how the records should be ordered.
func (cfq *CurrencyFeedQuery) Order(o ...currencyfeed.OrderOption) *CurrencyFeedQuery {
	cfq.order = append(cfq.order, o...)
	return cfq
}

// First returns the first CurrencyFeed entity from the query.
// Returns a *NotFoundError when no CurrencyFeed was found.
func (cfq *CurrencyFeedQuery) First(ctx context.Context) (*CurrencyFeed, error) {
	nodes, err := cfq.Limit(1).All(setContextOp(ctx, cfq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{currencyfeed.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cfq *CurrencyFeedQuery) FirstX(ctx context.Context) *CurrencyFeed {
	node, err := cfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CurrencyFeed ID from the query.
// Returns a *NotFoundError when no CurrencyFeed ID was found.
func (cfq *CurrencyFeedQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = cfq.Limit(1).IDs(setContextOp(ctx, cfq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{currencyfeed.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cfq *CurrencyFeedQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := cfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CurrencyFeed entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CurrencyFeed entity is found.
// Returns a *NotFoundError when no CurrencyFeed entities are found.
func (cfq *CurrencyFeedQuery) Only(ctx context.Context) (*CurrencyFeed, error) {
	nodes, err := cfq.Limit(2).All(setContextOp(ctx, cfq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{currencyfeed.Label}
	default:
		return nil, &NotSingularError{currencyfeed.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cfq *CurrencyFeedQuery) OnlyX(ctx context.Context) *CurrencyFeed {
	node, err := cfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CurrencyFeed ID in the query.
// Returns a *NotSingularError when more than one CurrencyFeed ID is found.
// Returns a *NotFoundError when no entities are found.
func (cfq *CurrencyFeedQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = cfq.Limit(2).IDs(setContextOp(ctx, cfq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{currencyfeed.Label}
	default:
		err = &NotSingularError{currencyfeed.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cfq *CurrencyFeedQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := cfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CurrencyFeeds.
func (cfq *CurrencyFeedQuery) All(ctx context.Context) ([]*CurrencyFeed, error) {
	ctx = setContextOp(ctx, cfq.ctx, ent.OpQueryAll)
	if err := cfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CurrencyFeed, *CurrencyFeedQuery]()
	return withInterceptors[[]*CurrencyFeed](ctx, cfq, qr, cfq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cfq *CurrencyFeedQuery) AllX(ctx context.Context) []*CurrencyFeed {
	nodes, err := cfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CurrencyFeed IDs.
func (cfq *CurrencyFeedQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if cfq.ctx.Unique == nil && cfq.path != nil {
		cfq.Unique(true)
	}
	ctx = setContextOp(ctx, cfq.ctx, ent.OpQueryIDs)
	if err = cfq.Select(currencyfeed.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cfq *CurrencyFeedQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := cfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cfq *CurrencyFeedQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cfq.ctx, ent.OpQueryCount)
	if err := cfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cfq, querierCount[*CurrencyFeedQuery](), cfq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cfq *CurrencyFeedQuery) CountX(ctx context.Context) int {
	count, err := cfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cfq *CurrencyFeedQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cfq.ctx, ent.OpQueryExist)
	switch _, err := cfq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("generated: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cfq *CurrencyFeedQuery) ExistX(ctx context.Context) bool {
	exist, err := cfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CurrencyFeedQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cfq *CurrencyFeedQuery) Clone() *CurrencyFeedQuery {
	if cfq == nil {
		return nil
	}
	return &CurrencyFeedQuery{
		config:     cfq.config,
		ctx:        cfq.ctx.Clone(),
		order:      append([]currencyfeed.OrderOption{}, cfq.order...),
		inters:     append([]Interceptor{}, cfq.inters...),
		predicates: append([]predicate.CurrencyFeed{}, cfq.predicates...),
		// clone intermediate query.
		sql:       cfq.sql.Clone(),
		path:      cfq.path,
		modifiers: append([]func(*sql.Selector){}, cfq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CurrencyFeed.Query().
//		GroupBy(currencyfeed.FieldCreatedAt).
//		Aggregate(generated.Count()).
//		Scan(ctx, &v)
func (cfq *CurrencyFeedQuery) GroupBy(field string, fields ...string) *CurrencyFeedGroupBy {
	cfq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CurrencyFeedGroupBy{build: cfq}
	grbuild.flds = &cfq.ctx.Fields
	grbuild.label = currencyfeed.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.CurrencyFeed.Query().
//		Select(currencyfeed.FieldCreatedAt).
//		Scan(ctx, &v)
func (cfq *CurrencyFeedQuery) Select(fields ...string) *CurrencyFeedSelect {
	cfq.ctx.Fields = append(cfq.ctx.Fields, fields...)
	sbuild := &CurrencyFeedSelect{CurrencyFeedQuery: cfq}
	sbuild.label = currencyfeed.Label
	sbuild.flds, sbuild.scan = &cfq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CurrencyFeedSelect configured with the given aggregations.
func (cfq *CurrencyFeedQuery) Aggregate(fns ...AggregateFunc) *CurrencyFeedSelect {
	return cfq.Select().Aggregate(fns...)
}

func (cfq *CurrencyFeedQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cfq.inters {
		if inter == nil {
			return fmt.Errorf("generated: uninitialized interceptor (forgotten import generated/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cfq); err != nil {
				return err
			}
		}
	}
	for _, f := range cfq.ctx.Fields {
		if !currencyfeed.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("generated: invalid field %q for query", f)}
		}
	}
	if cfq.path != nil {
		prev, err := cfq.path(ctx)
		if err != nil {
			return err
		}
		cfq.sql = prev
	}
	return nil
}

func (cfq *CurrencyFeedQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CurrencyFeed, error) {
	var (
		nodes = []*CurrencyFeed{}
		_spec = cfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CurrencyFeed).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CurrencyFeed{config: cfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(cfq.modifiers) > 0 {
		_spec.Modifiers = cfq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cfq *CurrencyFeedQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cfq.querySpec()
	if len(cfq.modifiers) > 0 {
		_spec.Modifiers = cfq.modifiers
	}
	_spec.Node.Columns = cfq.ctx.Fields
	if len(cfq.ctx.Fields) > 0 {
		_spec.Unique = cfq.ctx.Unique != nil && *cfq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cfq.driver, _spec)
}

func (cfq *CurrencyFeedQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(currencyfeed.Table, currencyfeed.Columns, sqlgraph.NewFieldSpec(currencyfeed.FieldID, field.TypeUint32))
	_spec.From = cfq.sql
	if unique := cfq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cfq.path != nil {
		_spec.Unique = true
	}
	if fields := cfq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, currencyfeed.FieldID)
		for i := range fields {
			if fields[i] != currencyfeed.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cfq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cfq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cfq *CurrencyFeedQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cfq.driver.Dialect())
	t1 := builder.Table(currencyfeed.Table)
	columns := cfq.ctx.Fields
	if len(columns) == 0 {
		columns = currencyfeed.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cfq.sql != nil {
		selector = cfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cfq.ctx.Unique != nil && *cfq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range cfq.modifiers {
		m(selector)
	}
	for _, p := range cfq.predicates {
		p(selector)
	}
	for _, p := range cfq.order {
		p(selector)
	}
	if offset := cfq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cfq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (cfq *CurrencyFeedQuery) ForUpdate(opts ...sql.LockOption) *CurrencyFeedQuery {
	if cfq.driver.Dialect() == dialect.Postgres {
		cfq.Unique(false)
	}
	cfq.modifiers = append(cfq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return cfq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (cfq *CurrencyFeedQuery) ForShare(opts ...sql.LockOption) *CurrencyFeedQuery {
	if cfq.driver.Dialect() == dialect.Postgres {
		cfq.Unique(false)
	}
	cfq.modifiers = append(cfq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return cfq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cfq *CurrencyFeedQuery) Modify(modifiers ...func(s *sql.Selector)) *CurrencyFeedSelect {
	cfq.modifiers = append(cfq.modifiers, modifiers...)
	return cfq.Select()
}

// CurrencyFeedGroupBy is the group-by builder for CurrencyFeed entities.
type CurrencyFeedGroupBy struct {
	selector
	build *CurrencyFeedQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cfgb *CurrencyFeedGroupBy) Aggregate(fns ...AggregateFunc) *CurrencyFeedGroupBy {
	cfgb.fns = append(cfgb.fns, fns...)
	return cfgb
}

// Scan applies the selector query and scans the result into the given value.
func (cfgb *CurrencyFeedGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cfgb.build.ctx, ent.OpQueryGroupBy)
	if err := cfgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CurrencyFeedQuery, *CurrencyFeedGroupBy](ctx, cfgb.build, cfgb, cfgb.build.inters, v)
}

func (cfgb *CurrencyFeedGroupBy) sqlScan(ctx context.Context, root *CurrencyFeedQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cfgb.fns))
	for _, fn := range cfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cfgb.flds)+len(cfgb.fns))
		for _, f := range *cfgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cfgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cfgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CurrencyFeedSelect is the builder for selecting fields of CurrencyFeed entities.
type CurrencyFeedSelect struct {
	*CurrencyFeedQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cfs *CurrencyFeedSelect) Aggregate(fns ...AggregateFunc) *CurrencyFeedSelect {
	cfs.fns = append(cfs.fns, fns...)
	return cfs
}

// Scan applies the selector query and scans the result into the given value.
func (cfs *CurrencyFeedSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cfs.ctx, ent.OpQuerySelect)
	if err := cfs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CurrencyFeedQuery, *CurrencyFeedSelect](ctx, cfs.CurrencyFeedQuery, cfs, cfs.inters, v)
}

func (cfs *CurrencyFeedSelect) sqlScan(ctx context.Context, root *CurrencyFeedQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cfs.fns))
	for _, fn := range cfs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cfs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cfs *CurrencyFeedSelect) Modify(modifiers ...func(s *sql.Selector)) *CurrencyFeedSelect {
	cfs.modifiers = append(cfs.modifiers, modifiers...)
	return cfs
}
