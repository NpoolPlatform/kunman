// Code generated by ent, DO NOT EDIT.

package generated

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/kunman/middleware/ledger/db/ent/generated/predicate"
	"github.com/NpoolPlatform/kunman/middleware/ledger/db/ent/generated/simulateledger"
)

// SimulateLedgerQuery is the builder for querying SimulateLedger entities.
type SimulateLedgerQuery struct {
	config
	ctx        *QueryContext
	order      []simulateledger.OrderOption
	inters     []Interceptor
	predicates []predicate.SimulateLedger
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SimulateLedgerQuery builder.
func (slq *SimulateLedgerQuery) Where(ps ...predicate.SimulateLedger) *SimulateLedgerQuery {
	slq.predicates = append(slq.predicates, ps...)
	return slq
}

// Limit the number of records to be returned by this query.
func (slq *SimulateLedgerQuery) Limit(limit int) *SimulateLedgerQuery {
	slq.ctx.Limit = &limit
	return slq
}

// Offset to start from.
func (slq *SimulateLedgerQuery) Offset(offset int) *SimulateLedgerQuery {
	slq.ctx.Offset = &offset
	return slq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (slq *SimulateLedgerQuery) Unique(unique bool) *SimulateLedgerQuery {
	slq.ctx.Unique = &unique
	return slq
}

// Order specifies how the records should be ordered.
func (slq *SimulateLedgerQuery) Order(o ...simulateledger.OrderOption) *SimulateLedgerQuery {
	slq.order = append(slq.order, o...)
	return slq
}

// First returns the first SimulateLedger entity from the query.
// Returns a *NotFoundError when no SimulateLedger was found.
func (slq *SimulateLedgerQuery) First(ctx context.Context) (*SimulateLedger, error) {
	nodes, err := slq.Limit(1).All(setContextOp(ctx, slq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{simulateledger.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (slq *SimulateLedgerQuery) FirstX(ctx context.Context) *SimulateLedger {
	node, err := slq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SimulateLedger ID from the query.
// Returns a *NotFoundError when no SimulateLedger ID was found.
func (slq *SimulateLedgerQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = slq.Limit(1).IDs(setContextOp(ctx, slq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{simulateledger.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (slq *SimulateLedgerQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := slq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SimulateLedger entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SimulateLedger entity is found.
// Returns a *NotFoundError when no SimulateLedger entities are found.
func (slq *SimulateLedgerQuery) Only(ctx context.Context) (*SimulateLedger, error) {
	nodes, err := slq.Limit(2).All(setContextOp(ctx, slq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{simulateledger.Label}
	default:
		return nil, &NotSingularError{simulateledger.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (slq *SimulateLedgerQuery) OnlyX(ctx context.Context) *SimulateLedger {
	node, err := slq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SimulateLedger ID in the query.
// Returns a *NotSingularError when more than one SimulateLedger ID is found.
// Returns a *NotFoundError when no entities are found.
func (slq *SimulateLedgerQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = slq.Limit(2).IDs(setContextOp(ctx, slq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{simulateledger.Label}
	default:
		err = &NotSingularError{simulateledger.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (slq *SimulateLedgerQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := slq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SimulateLedgers.
func (slq *SimulateLedgerQuery) All(ctx context.Context) ([]*SimulateLedger, error) {
	ctx = setContextOp(ctx, slq.ctx, ent.OpQueryAll)
	if err := slq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SimulateLedger, *SimulateLedgerQuery]()
	return withInterceptors[[]*SimulateLedger](ctx, slq, qr, slq.inters)
}

// AllX is like All, but panics if an error occurs.
func (slq *SimulateLedgerQuery) AllX(ctx context.Context) []*SimulateLedger {
	nodes, err := slq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SimulateLedger IDs.
func (slq *SimulateLedgerQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if slq.ctx.Unique == nil && slq.path != nil {
		slq.Unique(true)
	}
	ctx = setContextOp(ctx, slq.ctx, ent.OpQueryIDs)
	if err = slq.Select(simulateledger.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (slq *SimulateLedgerQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := slq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (slq *SimulateLedgerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, slq.ctx, ent.OpQueryCount)
	if err := slq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, slq, querierCount[*SimulateLedgerQuery](), slq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (slq *SimulateLedgerQuery) CountX(ctx context.Context) int {
	count, err := slq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (slq *SimulateLedgerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, slq.ctx, ent.OpQueryExist)
	switch _, err := slq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("generated: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (slq *SimulateLedgerQuery) ExistX(ctx context.Context) bool {
	exist, err := slq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SimulateLedgerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (slq *SimulateLedgerQuery) Clone() *SimulateLedgerQuery {
	if slq == nil {
		return nil
	}
	return &SimulateLedgerQuery{
		config:     slq.config,
		ctx:        slq.ctx.Clone(),
		order:      append([]simulateledger.OrderOption{}, slq.order...),
		inters:     append([]Interceptor{}, slq.inters...),
		predicates: append([]predicate.SimulateLedger{}, slq.predicates...),
		// clone intermediate query.
		sql:       slq.sql.Clone(),
		path:      slq.path,
		modifiers: append([]func(*sql.Selector){}, slq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SimulateLedger.Query().
//		GroupBy(simulateledger.FieldCreatedAt).
//		Aggregate(generated.Count()).
//		Scan(ctx, &v)
func (slq *SimulateLedgerQuery) GroupBy(field string, fields ...string) *SimulateLedgerGroupBy {
	slq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SimulateLedgerGroupBy{build: slq}
	grbuild.flds = &slq.ctx.Fields
	grbuild.label = simulateledger.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.SimulateLedger.Query().
//		Select(simulateledger.FieldCreatedAt).
//		Scan(ctx, &v)
func (slq *SimulateLedgerQuery) Select(fields ...string) *SimulateLedgerSelect {
	slq.ctx.Fields = append(slq.ctx.Fields, fields...)
	sbuild := &SimulateLedgerSelect{SimulateLedgerQuery: slq}
	sbuild.label = simulateledger.Label
	sbuild.flds, sbuild.scan = &slq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SimulateLedgerSelect configured with the given aggregations.
func (slq *SimulateLedgerQuery) Aggregate(fns ...AggregateFunc) *SimulateLedgerSelect {
	return slq.Select().Aggregate(fns...)
}

func (slq *SimulateLedgerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range slq.inters {
		if inter == nil {
			return fmt.Errorf("generated: uninitialized interceptor (forgotten import generated/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, slq); err != nil {
				return err
			}
		}
	}
	for _, f := range slq.ctx.Fields {
		if !simulateledger.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("generated: invalid field %q for query", f)}
		}
	}
	if slq.path != nil {
		prev, err := slq.path(ctx)
		if err != nil {
			return err
		}
		slq.sql = prev
	}
	return nil
}

func (slq *SimulateLedgerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SimulateLedger, error) {
	var (
		nodes = []*SimulateLedger{}
		_spec = slq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SimulateLedger).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SimulateLedger{config: slq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(slq.modifiers) > 0 {
		_spec.Modifiers = slq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, slq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (slq *SimulateLedgerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := slq.querySpec()
	if len(slq.modifiers) > 0 {
		_spec.Modifiers = slq.modifiers
	}
	_spec.Node.Columns = slq.ctx.Fields
	if len(slq.ctx.Fields) > 0 {
		_spec.Unique = slq.ctx.Unique != nil && *slq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, slq.driver, _spec)
}

func (slq *SimulateLedgerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(simulateledger.Table, simulateledger.Columns, sqlgraph.NewFieldSpec(simulateledger.FieldID, field.TypeUint32))
	_spec.From = slq.sql
	if unique := slq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if slq.path != nil {
		_spec.Unique = true
	}
	if fields := slq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, simulateledger.FieldID)
		for i := range fields {
			if fields[i] != simulateledger.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := slq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := slq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := slq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := slq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (slq *SimulateLedgerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(slq.driver.Dialect())
	t1 := builder.Table(simulateledger.Table)
	columns := slq.ctx.Fields
	if len(columns) == 0 {
		columns = simulateledger.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if slq.sql != nil {
		selector = slq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if slq.ctx.Unique != nil && *slq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range slq.modifiers {
		m(selector)
	}
	for _, p := range slq.predicates {
		p(selector)
	}
	for _, p := range slq.order {
		p(selector)
	}
	if offset := slq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := slq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (slq *SimulateLedgerQuery) ForUpdate(opts ...sql.LockOption) *SimulateLedgerQuery {
	if slq.driver.Dialect() == dialect.Postgres {
		slq.Unique(false)
	}
	slq.modifiers = append(slq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return slq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (slq *SimulateLedgerQuery) ForShare(opts ...sql.LockOption) *SimulateLedgerQuery {
	if slq.driver.Dialect() == dialect.Postgres {
		slq.Unique(false)
	}
	slq.modifiers = append(slq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return slq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (slq *SimulateLedgerQuery) Modify(modifiers ...func(s *sql.Selector)) *SimulateLedgerSelect {
	slq.modifiers = append(slq.modifiers, modifiers...)
	return slq.Select()
}

// SimulateLedgerGroupBy is the group-by builder for SimulateLedger entities.
type SimulateLedgerGroupBy struct {
	selector
	build *SimulateLedgerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (slgb *SimulateLedgerGroupBy) Aggregate(fns ...AggregateFunc) *SimulateLedgerGroupBy {
	slgb.fns = append(slgb.fns, fns...)
	return slgb
}

// Scan applies the selector query and scans the result into the given value.
func (slgb *SimulateLedgerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, slgb.build.ctx, ent.OpQueryGroupBy)
	if err := slgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SimulateLedgerQuery, *SimulateLedgerGroupBy](ctx, slgb.build, slgb, slgb.build.inters, v)
}

func (slgb *SimulateLedgerGroupBy) sqlScan(ctx context.Context, root *SimulateLedgerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(slgb.fns))
	for _, fn := range slgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*slgb.flds)+len(slgb.fns))
		for _, f := range *slgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*slgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := slgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SimulateLedgerSelect is the builder for selecting fields of SimulateLedger entities.
type SimulateLedgerSelect struct {
	*SimulateLedgerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sls *SimulateLedgerSelect) Aggregate(fns ...AggregateFunc) *SimulateLedgerSelect {
	sls.fns = append(sls.fns, fns...)
	return sls
}

// Scan applies the selector query and scans the result into the given value.
func (sls *SimulateLedgerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sls.ctx, ent.OpQuerySelect)
	if err := sls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SimulateLedgerQuery, *SimulateLedgerSelect](ctx, sls.SimulateLedgerQuery, sls, sls.inters, v)
}

func (sls *SimulateLedgerSelect) sqlScan(ctx context.Context, root *SimulateLedgerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sls.fns))
	for _, fn := range sls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sls *SimulateLedgerSelect) Modify(modifiers ...func(s *sql.Selector)) *SimulateLedgerSelect {
	sls.modifiers = append(sls.modifiers, modifiers...)
	return sls
}
